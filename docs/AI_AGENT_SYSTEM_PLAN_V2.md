# 🤖 AI 어시스턴트 시스템 구현 플랜 V2 (끝판왕)

> **"나를 복제한 AI 어시스턴트"** - 앱 전문가처럼 모든 질문에 답변하는 AI

---

## 📋 목차

1. [핵심 컨셉](#1-핵심-컨셉)
2. [AI 페르소나 설계](#2-ai-페르소나-설계)
3. [지식 베이스 시스템](#3-지식-베이스-시스템)
4. [대화 정책 (Answer Policy)](#4-대화-정책-answer-policy)
5. [에이전트 아키텍처](#5-에이전트-아키텍처)
6. [상세 에이전트 설계](#6-상세-에이전트-설계)
7. [UI/UX 설계](#7-uiux-설계)
8. [구현 계획](#8-구현-계획)
9. [프롬프트 엔지니어링](#9-프롬프트-엔지니어링)
10. [테스트 시나리오](#10-테스트-시나리오)

---

## 1. 핵심 컨셉

### 1.1 비전

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   "이 앱의 모든 것을 알고 있는 나의 분신"                    │
│                                                             │
│   • 앱 사용법? → 즉시 답변                                  │
│   • 설정 방법? → 단계별 가이드                              │
│   • 기능 질문? → 상세 설명 + 예시                           │
│   • 글 작성? → 바로 생성                                    │
│   • 관련 없는 질문? → 정중히 거절                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 핵심 원칙

| 원칙 | 설명 |
|------|------|
| 🎯 **전문성** | 이 앱의 모든 기능을 완벽히 이해 |
| 🛡️ **경계** | 앱과 무관한 질문은 정중히 거절 |
| 💬 **친근함** | 사용자와 자연스러운 대화 |
| ⚡ **실행력** | 질문뿐 아니라 실제 작업 수행 |
| 📚 **교육** | 사용법을 친절하게 가르쳐줌 |

### 1.3 AI가 할 수 있는 것 vs 없는 것

#### ✅ 할 수 있는 것
```
• 앱 사용법 설명 (모든 기능)
• API 키 설정 방법 안내
• 글 생성/수정/발행
• 이미지 생성/검색
• 트렌드 분석
• 에러 해결 가이드
• 기능 추천 및 팁
• 설정 최적화 조언
```

#### ❌ 하지 않는 것
```
• 일반 상식 질문 (날씨, 뉴스 등)
• 코딩/프로그래밍 질문
• 개인적인 조언 (연애, 진로 등)
• 다른 앱/서비스 사용법
• 정치/종교/민감한 주제
• 앱과 무관한 창작 요청
```

---

## 2. AI 페르소나 설계

### 2.1 페르소나 정의

```typescript
interface AIPersona {
  name: '블로그 어시스턴트';
  personality: {
    tone: '친근하고 전문적인';
    style: '간결하지만 필요시 상세하게';
    emoji: '적절히 사용 (과하지 않게)';
  };
  expertise: [
    '네이버 블로그 자동화',
    '콘텐츠 생성 (Gemini API)',
    'SEO 최적화',
    '이미지 생성/검색',
    '앱 설정 및 트러블슈팅'
  ];
  boundaries: {
    scope: '이 앱 관련 질문만';
    refusal: '정중하지만 단호하게';
  };
}
```

### 2.2 대화 스타일 예시

#### 👍 좋은 예시
```
👤: "API 키 어떻게 설정해?"
🤖: "Gemini API 키 설정 방법 알려드릴게요!

1. 환경설정 탭으로 이동
2. 'Gemini API 키' 입력란에 키 입력
3. 저장 버튼 클릭

💡 API 키가 없으시면 '발급 가이드' 버튼을 눌러주세요!"
```

```
👤: "글이 왜 안 생성돼?"
🤖: "몇 가지 확인해볼게요:

✅ API 키가 올바르게 입력되었나요?
✅ 인터넷 연결이 정상인가요?
✅ Gemini 서버 상태가 정상인가요?

어떤 에러 메시지가 나오나요? 알려주시면 정확히 도와드릴게요!"
```

#### 👎 거절 예시
```
👤: "오늘 날씨 어때?"
🤖: "죄송해요, 저는 이 앱 사용에 관한 질문만 도와드릴 수 있어요 😊

대신 이런 건 도와드릴 수 있어요:
• 글 생성하는 방법
• API 키 설정
• 기능 사용법

뭐든 물어보세요!"
```

```
👤: "파이썬으로 크롤러 만들어줘"
🤖: "프로그래밍 질문은 제 전문 분야가 아니에요 😅

이 앱에서 URL 크롤링이 필요하시면:
• URL 입력 → 자동으로 내용 분석
• 글 생성까지 한 번에!

이 기능 사용법 알려드릴까요?"
```

---

## 3. 지식 베이스 시스템

### 3.1 지식 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    📚 지식 베이스 (Knowledge Base)           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 📖 앱 매뉴얼  │  │ ⚙️ 설정 가이드 │  │ 🔧 트러블슈팅 │      │
│  │              │  │              │  │              │      │
│  │ • 전체 기능  │  │ • API 키     │  │ • 에러 코드  │      │
│  │ • 사용법    │  │ • 환경설정   │  │ • 해결방법   │      │
│  │ • 단축키    │  │ • 고급설정   │  │ • FAQ       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 📝 글 작성   │  │ 🖼️ 이미지    │  │ 📊 분석      │      │
│  │              │  │              │  │              │      │
│  │ • SEO 모드  │  │ • 생성 방법  │  │ • 트렌드    │      │
│  │ • 홈피드    │  │ • 소스 설정  │  │ • 키워드    │      │
│  │ • 카테고리  │  │ • 저장 경로  │  │ • 데이터랩  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 지식 베이스 파일 구조

```
src/agents/knowledge/
├── index.ts                    # 지식 베이스 로더
├── app-manual.json             # 앱 전체 매뉴얼
├── settings-guide.json         # 설정 가이드
├── troubleshooting.json        # 트러블슈팅
├── features/
│   ├── content-generation.json # 글 생성 관련
│   ├── image-generation.json   # 이미지 생성 관련
│   ├── publishing.json         # 발행 관련
│   └── analytics.json          # 분석 관련
└── faq.json                    # 자주 묻는 질문
```

### 3.3 지식 베이스 스키마

```typescript
interface KnowledgeItem {
  id: string;
  category: 'manual' | 'settings' | 'troubleshooting' | 'feature' | 'faq';
  keywords: string[];           // 검색 키워드
  question?: string;            // FAQ 형식일 경우
  title: string;
  content: string;
  steps?: string[];             // 단계별 가이드
  tips?: string[];              // 추가 팁
  relatedTopics?: string[];     // 관련 주제
  lastUpdated: string;
}

// 예시
const apiKeyGuide: KnowledgeItem = {
  id: 'settings-gemini-api',
  category: 'settings',
  keywords: ['api', 'gemini', '키', '설정', 'api키', '제미나이'],
  title: 'Gemini API 키 설정',
  content: 'Gemini API 키를 설정하면 AI 글 생성과 이미지 생성 기능을 사용할 수 있습니다.',
  steps: [
    '1. 상단 메뉴에서 "환경설정" 탭을 클릭합니다.',
    '2. "Gemini API 키" 입력란을 찾습니다.',
    '3. 발급받은 API 키를 입력합니다.',
    '4. "저장" 버튼을 클릭합니다.',
    '5. 앱을 재시작하면 설정이 적용됩니다.'
  ],
  tips: [
    '💡 API 키가 없으시면 "발급 가이드" 버튼을 눌러 Google AI Studio에서 무료로 발급받으세요.',
    '💡 무료 티어로 하루 1,500회 요청이 가능합니다.'
  ],
  relatedTopics: ['settings-gemini-model', 'troubleshooting-api-error'],
  lastUpdated: '2024-12-17'
};
```

### 3.4 지식 검색 시스템

```typescript
class KnowledgeSearch {
  private knowledge: KnowledgeItem[];
  
  // 키워드 기반 검색
  search(query: string): KnowledgeItem[] {
    const keywords = this.extractKeywords(query);
    return this.knowledge
      .filter(item => this.matchScore(item, keywords) > 0.5)
      .sort((a, b) => this.matchScore(b, keywords) - this.matchScore(a, keywords));
  }
  
  // 카테고리별 조회
  getByCategory(category: string): KnowledgeItem[];
  
  // 관련 주제 조회
  getRelated(itemId: string): KnowledgeItem[];
}
```

---

## 4. 대화 정책 (Answer Policy)

### 4.1 질문 분류 시스템

```typescript
type QuestionCategory = 
  | 'APP_USAGE'        // 앱 사용법
  | 'SETTINGS'         // 설정 관련
  | 'FEATURE'          // 기능 설명
  | 'TROUBLESHOOTING'  // 문제 해결
  | 'ACTION_REQUEST'   // 작업 요청 (글 생성 등)
  | 'OUT_OF_SCOPE'     // 범위 밖 질문
  | 'AMBIGUOUS';       // 모호한 질문

interface ClassificationResult {
  category: QuestionCategory;
  confidence: number;
  suggestedAction: 'answer' | 'clarify' | 'refuse' | 'execute';
  matchedKeywords: string[];
}
```

### 4.2 범위 판단 로직

```typescript
const SCOPE_KEYWORDS = {
  // ✅ 범위 내 (답변 가능)
  inScope: [
    // 앱 기능
    '글', '생성', '작성', '발행', '이미지', '사진',
    '블로그', '네이버', 'SEO', '홈피드', '크롤링',
    // 설정
    '설정', 'API', '키', 'Gemini', '제미나이', '환경설정',
    '저장', '경로', '모델', '무료', '유료',
    // 사용법
    '어떻게', '방법', '사용', '하는법', '뭐야', '뭔가요',
    // 문제
    '안돼', '실패', '에러', '오류', '문제', '왜',
    // 앱 관련
    '앱', '프로그램', '기능', '버튼', '메뉴', '탭'
  ],
  
  // ❌ 범위 밖 (거절)
  outOfScope: [
    // 일반 질문
    '날씨', '뉴스', '주식', '코인', '환율',
    // 다른 분야
    '코딩', '프로그래밍', '파이썬', '자바',
    // 개인적 질문
    '연애', '진로', '취업', '면접',
    // 기타
    '게임', '영화', '음악', '맛집', '여행'
  ]
};

function classifyQuestion(question: string): ClassificationResult {
  const lowerQuestion = question.toLowerCase();
  
  // 1. 명시적 범위 밖 키워드 체크
  const outOfScopeMatch = SCOPE_KEYWORDS.outOfScope
    .filter(kw => lowerQuestion.includes(kw));
  
  if (outOfScopeMatch.length > 0) {
    return {
      category: 'OUT_OF_SCOPE',
      confidence: 0.9,
      suggestedAction: 'refuse',
      matchedKeywords: outOfScopeMatch
    };
  }
  
  // 2. 범위 내 키워드 매칭
  const inScopeMatch = SCOPE_KEYWORDS.inScope
    .filter(kw => lowerQuestion.includes(kw));
  
  if (inScopeMatch.length >= 2) {
    // 작업 요청인지 질문인지 구분
    if (/해줘|해주세요|만들어|생성해|써줘/.test(lowerQuestion)) {
      return {
        category: 'ACTION_REQUEST',
        confidence: 0.85,
        suggestedAction: 'execute',
        matchedKeywords: inScopeMatch
      };
    }
    return {
      category: 'APP_USAGE',
      confidence: 0.8,
      suggestedAction: 'answer',
      matchedKeywords: inScopeMatch
    };
  }
  
  // 3. 모호한 경우
  return {
    category: 'AMBIGUOUS',
    confidence: 0.5,
    suggestedAction: 'clarify',
    matchedKeywords: inScopeMatch
  };
}
```

### 4.3 거절 응답 템플릿

```typescript
const REFUSAL_TEMPLATES = {
  general: `
죄송해요, 저는 이 앱 사용에 관한 질문만 도와드릴 수 있어요 😊

대신 이런 건 도와드릴 수 있어요:
• 📝 글 생성/수정/발행
• 🖼️ 이미지 생성
• ⚙️ 설정 방법
• 🔧 문제 해결

무엇을 도와드릴까요?
  `,
  
  coding: `
프로그래밍 관련 질문은 제 전문 분야가 아니에요 😅

하지만 이 앱에서는:
• URL 입력만으로 자동 크롤링
• AI가 글을 자동으로 생성

별도 코딩 없이 사용 가능해요! 사용법 알려드릴까요?
  `,
  
  personal: `
개인적인 질문에는 답변드리기 어려워요 😊

저는 블로그 자동화 전문이에요!
• 글 작성 도움
• SEO 최적화
• 이미지 생성

이런 건 언제든 물어보세요!
  `
};
```

---

## 5. 에이전트 아키텍처

### 5.1 전체 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 메시지                            │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    🎯 마스터 에이전트 (Master)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. 질문 분류 (Classifier)                                │   │
│  │    → IN_SCOPE / OUT_OF_SCOPE / ACTION_REQUEST            │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 2. 라우팅 결정                                           │   │
│  │    → 적절한 서브 에이전트 선택                            │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 3. 결과 통합 및 응답 생성                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        ▼                       ▼                       ▼
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│ 📚 지식 에이전트  │   │ ⚡ 실행 에이전트  │   │ 🛡️ 거절 에이전트  │
│                  │   │                  │   │                  │
│ • 사용법 설명    │   │ • 글 생성        │   │ • 정중한 거절    │
│ • 설정 가이드    │   │ • 이미지 생성    │   │ • 대안 제시      │
│ • FAQ 응답      │   │ • 발행 실행      │   │ • 범위 안내      │
│ • 트러블슈팅    │   │ • 분석 실행      │   │                  │
└──────────────────┘   └──────────────────┘   └──────────────────┘
        │                       │
        ▼                       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        📖 지식 베이스                             │
│   (앱 매뉴얼, 설정 가이드, FAQ, 트러블슈팅)                        │
└──────────────────────────────────────────────────────────────────┘
```

### 5.2 에이전트 간 통신

```typescript
interface AgentMessage {
  from: string;           // 발신 에이전트
  to: string;             // 수신 에이전트
  type: 'request' | 'response' | 'delegate';
  payload: any;
  context: ChatContext;
}

interface AgentResult {
  success: boolean;
  response?: string;
  actions?: ActionResult[];
  suggestFollowUp?: string[];
  error?: string;
}
```

---

## 6. 상세 에이전트 설계

### 6.1 마스터 에이전트 (Master Agent)

```typescript
class MasterAgent {
  private classifier: QuestionClassifier;
  private knowledgeAgent: KnowledgeAgent;
  private executionAgent: ExecutionAgent;
  private refusalAgent: RefusalAgent;
  private context: ChatContext;
  
  async processMessage(message: string): Promise<AgentResult> {
    // 1. 질문 분류
    const classification = await this.classifier.classify(message, this.context);
    
    // 2. 라우팅
    switch (classification.category) {
      case 'OUT_OF_SCOPE':
        return this.refusalAgent.handle(message, classification);
        
      case 'ACTION_REQUEST':
        return this.executionAgent.handle(message, classification);
        
      case 'APP_USAGE':
      case 'SETTINGS':
      case 'FEATURE':
      case 'TROUBLESHOOTING':
        return this.knowledgeAgent.handle(message, classification);
        
      case 'AMBIGUOUS':
        return this.handleAmbiguous(message);
    }
  }
  
  private async handleAmbiguous(message: string): Promise<AgentResult> {
    return {
      success: true,
      response: `
무슨 도움이 필요하신지 좀 더 알려주시겠어요?

예를 들어:
• "글 생성 방법 알려줘"
• "API 키 설정은 어떻게 해?"
• "이 주제로 글 써줘"
      `,
      suggestFollowUp: ['글 생성 방법', 'API 키 설정', '기능 소개']
    };
  }
}
```

### 6.2 지식 에이전트 (Knowledge Agent)

```typescript
class KnowledgeAgent extends BaseAgent {
  name = 'knowledge';
  description = '앱 사용법, 설정, 기능에 대한 질문 응답';
  
  private knowledgeBase: KnowledgeSearch;
  
  systemPrompt = `
당신은 "리더 네이버 자동화" 앱의 전문 어시스턴트입니다.

## 역할
- 앱 사용법을 친절하게 설명합니다
- 설정 방법을 단계별로 안내합니다
- 문제 해결을 도와줍니다

## 응답 스타일
- 친근하고 전문적인 톤
- 단계별 가이드 제공 (필요시)
- 이모지 적절히 사용
- 추가 팁이 있으면 제공

## 지식 베이스
다음 정보를 바탕으로 답변하세요:
{knowledgeContext}

## 주의사항
- 모르는 내용은 "확인이 필요해요"라고 정직하게 말하기
- 추측으로 답변하지 않기
- 관련 기능 추천하기
  `;
  
  async handle(message: string, classification: ClassificationResult): Promise<AgentResult> {
    // 1. 지식 베이스에서 관련 정보 검색
    const relevantKnowledge = this.knowledgeBase.search(message);
    
    // 2. 컨텍스트 구성
    const knowledgeContext = this.formatKnowledge(relevantKnowledge);
    
    // 3. Gemini로 응답 생성
    const prompt = this.systemPrompt.replace('{knowledgeContext}', knowledgeContext);
    const response = await this.callGemini(prompt, message);
    
    return {
      success: true,
      response,
      suggestFollowUp: relevantKnowledge.slice(0, 3).map(k => k.title)
    };
  }
}
```

### 6.3 실행 에이전트 (Execution Agent)

```typescript
class ExecutionAgent extends BaseAgent {
  name = 'execution';
  description = '글 생성, 이미지 생성 등 실제 작업 수행';
  
  // 서브 에이전트들
  private writerAgent: WriterAgent;
  private imageAgent: ImageAgent;
  private editorAgent: EditorAgent;
  private publisherAgent: PublisherAgent;
  private analyzerAgent: AnalyzerAgent;
  
  systemPrompt = `
당신은 작업 수행 전문가입니다.

## 역할
- 사용자 요청을 분석하여 적절한 작업 수행
- 작업 진행 상황 안내
- 결과물 제시 및 다음 단계 제안

## 수행 가능한 작업
1. 글 생성 (주제/URL/키워드 기반)
2. 글 수정 (제목, 본문)
3. 이미지 생성/검색
4. 블로그 발행
5. 트렌드/키워드 분석

## 응답 형식
- 작업 시작 알림
- 진행 상황 표시
- 결과물 + 액션 버튼
  `;
  
  async handle(message: string, classification: ClassificationResult): Promise<AgentResult> {
    // 1. 작업 유형 파악
    const actionType = this.detectActionType(message);
    
    // 2. 해당 서브 에이전트 실행
    switch (actionType) {
      case 'WRITE':
        return this.writerAgent.execute(message);
      case 'EDIT':
        return this.editorAgent.execute(message);
      case 'IMAGE':
        return this.imageAgent.execute(message);
      case 'PUBLISH':
        return this.publisherAgent.execute(message);
      case 'ANALYZE':
        return this.analyzerAgent.execute(message);
    }
  }
  
  private detectActionType(message: string): ActionType {
    if (/글|작성|써/.test(message)) return 'WRITE';
    if (/수정|바꿔|고쳐/.test(message)) return 'EDIT';
    if (/이미지|사진|그림/.test(message)) return 'IMAGE';
    if (/발행|게시|올려/.test(message)) return 'PUBLISH';
    if (/분석|트렌드|키워드/.test(message)) return 'ANALYZE';
    return 'WRITE'; // 기본값
  }
}
```

### 6.4 거절 에이전트 (Refusal Agent)

```typescript
class RefusalAgent extends BaseAgent {
  name = 'refusal';
  description = '범위 밖 질문에 정중하게 거절';
  
  systemPrompt = `
당신은 질문이 앱 범위를 벗어날 때 정중하게 거절하는 역할입니다.

## 원칙
1. 정중하지만 단호하게
2. 왜 답변할 수 없는지 간단히 설명
3. 대신 할 수 있는 것 제안
4. 적절한 이모지로 부드럽게

## 거절 카테고리별 응답
- 일반 질문: 앱 범위 밖임을 안내
- 코딩/개발: 앱의 자동화 기능 소개
- 개인 질문: 블로그 관련으로 유도

## 주의
- 무례하지 않게
- 너무 길지 않게
- 항상 대안 제시
  `;
  
  async handle(message: string, classification: ClassificationResult): Promise<AgentResult> {
    const refusalType = this.detectRefusalType(classification.matchedKeywords);
    const template = REFUSAL_TEMPLATES[refusalType] || REFUSAL_TEMPLATES.general;
    
    return {
      success: true,
      response: template,
      suggestFollowUp: ['앱 사용법', 'API 키 설정', '글 생성 방법']
    };
  }
}
```

---

## 7. UI/UX 설계

### 7.1 채팅 패널 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│  🤖 AI 어시스턴트                                    [─] [□] [×] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 👋 안녕하세요! 저는 블로그 자동화 어시스턴트예요.        │   │
│  │                                                         │   │
│  │ 이런 것들을 도와드릴 수 있어요:                          │   │
│  │ • 📝 글 생성/수정                                       │   │
│  │ • 🖼️ 이미지 생성                                        │   │
│  │ • ⚙️ 설정 방법 안내                                     │   │
│  │ • 🔧 문제 해결                                          │   │
│  │                                                         │   │
│  │ 무엇을 도와드릴까요?                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 👤 API 키 어떻게 설정해?                                  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 🤖 Gemini API 키 설정 방법 알려드릴게요!                  │ │
│  │                                                           │ │
│  │ 1️⃣ 환경설정 탭으로 이동                                  │ │
│  │ 2️⃣ 'Gemini API 키' 입력란 찾기                          │ │
│  │ 3️⃣ API 키 입력                                          │ │
│  │ 4️⃣ 저장 버튼 클릭                                       │ │
│  │                                                           │ │
│  │ 💡 Tip: API 키가 없으시면 '발급 가이드' 버튼을 눌러       │ │
│  │         Google AI Studio에서 무료로 발급받으세요!         │ │
│  │                                                           │ │
│  │ ┌──────────────────┐ ┌──────────────────┐               │ │
│  │ │ 🔗 환경설정 열기  │ │ 📖 발급 가이드   │               │ │
│  │ └──────────────────┘ └──────────────────┘               │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  💬 빠른 질문:  [사용법] [설정] [글 생성] [문제 해결]           │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐ [전송] │
│  │ 메시지를 입력하세요...                               │       │
│  └─────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 컴포넌트 상세

#### 7.2.1 메시지 컴포넌트
```html
<!-- 사용자 메시지 -->
<div class="chat-message user">
  <div class="message-content">
    <span class="message-text">API 키 어떻게 설정해?</span>
  </div>
  <span class="message-time">오후 2:30</span>
</div>

<!-- AI 메시지 -->
<div class="chat-message assistant">
  <div class="message-avatar">🤖</div>
  <div class="message-content">
    <div class="message-text">...</div>
    
    <!-- 액션 버튼 (선택적) -->
    <div class="message-actions">
      <button class="action-btn primary">🔗 환경설정 열기</button>
      <button class="action-btn secondary">📖 발급 가이드</button>
    </div>
  </div>
  <span class="message-time">오후 2:31</span>
</div>

<!-- 작업 진행 중 -->
<div class="chat-message assistant working">
  <div class="message-avatar">⚙️</div>
  <div class="message-content">
    <div class="agent-badge">📝 글 생성 에이전트</div>
    <div class="message-text">글을 작성하고 있어요...</div>
    <div class="progress-bar">
      <div class="progress-fill" style="width: 45%"></div>
    </div>
  </div>
</div>
```

#### 7.2.2 빠른 질문 버튼
```html
<div class="quick-questions">
  <button class="quick-btn" data-question="앱 사용법 알려줘">
    📖 사용법
  </button>
  <button class="quick-btn" data-question="API 키 설정 방법">
    ⚙️ 설정
  </button>
  <button class="quick-btn" data-question="글 생성해줘">
    📝 글 생성
  </button>
  <button class="quick-btn" data-question="에러가 발생했어">
    🔧 문제 해결
  </button>
</div>
```

### 7.3 스타일 (CSS)

```css
/* 채팅 패널 */
.chat-panel {
  display: flex;
  flex-direction: column;
  height: 100%;
  background: var(--bg-primary);
  border-left: 1px solid var(--border-medium);
}

/* 메시지 영역 */
.chat-messages {
  flex: 1;
  overflow-y: auto;
  padding: 1rem;
}

/* 사용자 메시지 */
.chat-message.user {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 1rem;
}

.chat-message.user .message-content {
  background: linear-gradient(135deg, #D4AF37, #FFD700);
  color: #1a1a2e;
  padding: 0.75rem 1rem;
  border-radius: 18px 18px 4px 18px;
  max-width: 80%;
}

/* AI 메시지 */
.chat-message.assistant {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 1rem;
}

.chat-message.assistant .message-avatar {
  width: 36px;
  height: 36px;
  font-size: 1.5rem;
}

.chat-message.assistant .message-content {
  background: var(--bg-secondary);
  padding: 0.75rem 1rem;
  border-radius: 4px 18px 18px 18px;
  max-width: 80%;
}

/* 작업 진행 표시 */
.agent-badge {
  display: inline-block;
  background: rgba(212, 175, 55, 0.2);
  color: var(--text-gold);
  padding: 0.25rem 0.5rem;
  border-radius: 4px;
  font-size: 0.8rem;
  margin-bottom: 0.5rem;
}

.progress-bar {
  height: 4px;
  background: var(--bg-tertiary);
  border-radius: 2px;
  margin-top: 0.5rem;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #D4AF37, #FFD700);
  border-radius: 2px;
  transition: width 0.3s ease;
}

/* 액션 버튼 */
.message-actions {
  display: flex;
  gap: 0.5rem;
  margin-top: 0.75rem;
}

.action-btn {
  padding: 0.5rem 1rem;
  border-radius: 8px;
  font-size: 0.85rem;
  cursor: pointer;
  border: none;
}

.action-btn.primary {
  background: linear-gradient(135deg, #D4AF37, #FFD700);
  color: #1a1a2e;
}

.action-btn.secondary {
  background: var(--bg-tertiary);
  color: var(--text-strong);
  border: 1px solid var(--border-medium);
}

/* 빠른 질문 */
.quick-questions {
  display: flex;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  border-top: 1px solid var(--border-medium);
  overflow-x: auto;
}

.quick-btn {
  padding: 0.4rem 0.8rem;
  border-radius: 16px;
  background: var(--bg-secondary);
  color: var(--text-muted);
  border: 1px solid var(--border-medium);
  font-size: 0.8rem;
  white-space: nowrap;
  cursor: pointer;
}

.quick-btn:hover {
  border-color: var(--text-gold);
  color: var(--text-gold);
}
```

---

## 8. 구현 계획

### 8.1 Phase 1: 핵심 인프라 (2일)

#### Day 1: 기반 구조
```
□ src/agents/index.ts          - 에이전트 시스템 진입점
□ src/agents/baseAgent.ts      - BaseAgent 클래스
□ src/agents/types.ts          - 타입 정의
□ src/agents/chatContext.ts    - 대화 컨텍스트
```

#### Day 2: 지식 베이스
```
□ src/agents/knowledge/        - 지식 베이스 폴더
□ 지식 데이터 작성 (JSON)
□ 검색 시스템 구현
```

### 8.2 Phase 2: 에이전트 구현 (3일)

#### Day 3: 마스터 + 분류기
```
□ src/agents/masterAgent.ts    - 마스터 에이전트
□ src/agents/classifier.ts     - 질문 분류기
□ 분류 정확도 테스트
```

#### Day 4: 지식 + 거절 에이전트
```
□ src/agents/knowledgeAgent.ts - 지식 에이전트
□ src/agents/refusalAgent.ts   - 거절 에이전트
□ 응답 품질 테스트
```

#### Day 5: 실행 에이전트
```
□ src/agents/executionAgent.ts - 실행 에이전트
□ src/agents/writers/          - 서브 에이전트들
□ 기존 기능 연동
```

### 8.3 Phase 3: UI 구현 (2일)

#### Day 6: 채팅 패널
```
□ public/index.html            - 채팅 패널 HTML
□ src/renderer/chatPanel.ts    - 채팅 로직
□ CSS 스타일링
```

#### Day 7: IPC 연동
```
□ src/main.ts                  - IPC 핸들러
□ 실시간 메시지 처리
□ 액션 버튼 연동
```

### 8.4 Phase 4: 고급 기능 (2일)

#### Day 8: 대화 히스토리
```
□ 대화 저장/불러오기
□ 세션 관리
□ 컨텍스트 유지
```

#### Day 9: 테스트 & 최적화
```
□ 통합 테스트
□ 응답 속도 최적화
□ 에러 핸들링
```

---

## 9. 프롬프트 엔지니어링

### 9.1 마스터 에이전트 시스템 프롬프트

```typescript
const MASTER_SYSTEM_PROMPT = `
# 역할
당신은 "리더 네이버 자동화" 앱의 AI 어시스턴트입니다.
앱 개발자를 복제한 전문가로서, 앱의 모든 것을 알고 있습니다.

# 성격
- 친근하고 전문적
- 간결하지만 필요시 상세하게
- 이모지 적절히 사용 (과하지 않게)
- 항상 도움이 되려고 노력

# 핵심 원칙

## 1. 범위 엄수
✅ 답변 가능:
- 앱 사용법, 기능 설명
- API 키 설정, 환경설정
- 글 생성, 이미지 생성, 발행
- 에러 해결, 트러블슈팅
- SEO, 블로그 관련 팁

❌ 답변 불가:
- 일반 상식 (날씨, 뉴스 등)
- 프로그래밍/코딩 질문
- 개인적 조언 (연애, 진로 등)
- 다른 앱/서비스 사용법
- 앱과 무관한 창작 요청

## 2. 거절 방식
범위 밖 질문 시:
1. 정중하게 할 수 없다고 말하기
2. 이유 간단히 설명
3. 대신 할 수 있는 것 제안
4. 너무 길지 않게

예시:
"죄송해요, 저는 이 앱 관련 질문만 도와드릴 수 있어요 😊
대신 글 생성, 설정 방법 등은 언제든 물어보세요!"

## 3. 응답 스타일
- 단계별 가이드: 번호 매겨서
- 팁 제공: 💡 이모지와 함께
- 관련 기능 추천
- 추가 질문 유도

# 앱 정보
- 이름: 리더 네이버 자동화
- 기능: 네이버 블로그 자동 글 생성/발행
- AI: Gemini API 사용
- 이미지: Gemini Imagen, 무료 이미지 검색
`;
```

### 9.2 지식 에이전트 프롬프트

```typescript
const KNOWLEDGE_AGENT_PROMPT = `
당신은 앱 사용법 전문가입니다.

## 역할
- 사용자 질문에 정확하게 답변
- 단계별 가이드 제공
- 관련 팁과 주의사항 안내

## 응답 형식
1. 핵심 답변 (1-2문장)
2. 상세 설명 또는 단계 (필요시)
3. 추가 팁 (💡)
4. 관련 기능 추천 (선택)

## 지식 베이스
{knowledgeContext}

## 주의
- 지식 베이스에 없는 내용은 추측하지 않기
- 확실하지 않으면 "확인이 필요해요"라고 하기
- 항상 친근한 톤 유지
`;
```

---

## 10. 테스트 시나리오

### 10.1 기능 테스트

#### 사용법 질문
```
입력: "API 키 어떻게 설정해?"
기대: 단계별 설정 가이드 + 팁

입력: "글 생성은 어떻게 해?"
기대: 3가지 방법 (URL/키워드/직접 입력) 설명

입력: "이미지 어디서 가져와?"
기대: 이미지 소스 설명 + 설정 방법
```

#### 작업 요청
```
입력: "겨울 건강 팁으로 글 써줘"
기대: 글 생성 시작 → 결과 + 적용 버튼

입력: "제목 더 강렬하게 바꿔줘"
기대: 수정된 제목 제안 + 적용/다시 생성 버튼
```

#### 범위 밖 질문
```
입력: "오늘 날씨 어때?"
기대: 정중한 거절 + 앱 기능 안내

입력: "파이썬 코드 짜줘"
기대: 거절 + 앱의 자동화 기능 소개

입력: "연애 상담해줘"
기대: 거절 + 블로그 관련 도움 제안
```

### 10.2 품질 체크리스트

```
□ 응답 시간 < 3초
□ 거절 응답이 정중한가?
□ 단계별 가이드가 명확한가?
□ 액션 버튼이 작동하는가?
□ 대화 컨텍스트가 유지되는가?
□ 에러 발생 시 적절히 처리하는가?
```

---

## ✅ 다음 단계

플랜 검토 후 구현 시작!

**권장 순서:**
1. ⭐ Phase 1: 지식 베이스부터 (앱 정보 정리)
2. ⭐ Phase 2: 마스터 에이전트 + 분류기
3. ⭐ Phase 3: UI 구현
4. Phase 4: 고급 기능

**시작할까요?** 🚀
