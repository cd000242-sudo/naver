# 품질 하네스 — 2026-09-02

실측 글에서 나온 결함을 순서대로 잡는다. 새 세션이 이 파일만 읽고 이어받을 수 있어야 한다.

## 이 하네스를 쓰는 법

1. **P 번호 순서대로** 한 건씩 잡는다. 건너뛰지 않는다.
2. 한 건이 끝나면 **커밋 1개 + 전체 회귀 GREEN** 을 확인하고 다음으로 간다.
3. 상태를 이 파일에 갱신한다. 끝났으면 `✅`, 막혔으면 `⛔` 와 이유.
4. **라이브 검증 장부**(맨 아래)에 실측 결과를 적는다. 코드 통과는 검증이 아니다.

## 절대 규칙 (사장님 지시 — 어기면 되돌린다)

| 규칙 | 뜻 |
|---|---|
| **하드코딩 금지** | 실측 글은 *증상을 찾는 근거*다. 고치는 규칙은 그 글이 없어도 성립해야 한다. 낱말을 나열하지 말고 형태로 가른다 |
| **1릴리즈당 1~3 fix** | 회귀 cascade 금지. 커밋을 쪼갠다 |
| **발행 차단 게이트 신설 금지** | 경고만. 근거 게이트 완화도 금지 |
| **감지 후 재작성 금지** | 비용만 늘고 효과 없다. 첫 호출에서 지키게 한다 |
| **자동 폴백 금지** | 모델·엔진이 안 맞으면 조용히 바꾸지 말고 멈추고 알린다 |
| **저비용 유지** | frontier 업그레이드는 답이 아니다 |
| **`src/crawler/smartCrawler.ts` 손대지 않는다** | 타 세션 미커밋 작업 |
| **릴리즈는 메인 트리에서만** | 격리 워크트리는 CRLF라 지문 테스트가 깨진다 |

## 반복 확인된 실패 유형 (진단할 때 먼저 의심할 것)

1. **산문은 흘리고 스키마 필드는 지킨다** — 4만 자 프롬프트 끝의 산문은 배경이 된다
2. **금지만 있고 대안이 없으면 모델이 임의로 고른다**
3. **같은 규칙이 여러 곳에 복사돼 한 곳만 고쳐진다** — 제목엔 넣고 본문엔 안 넣음 / 크롤러엔 붙이고 지식iN엔 안 붙임
4. **감지기가 경고만 내고 출력을 안 바꾼다** — 잡고도 그대로 나간다
5. **모델에게 시키는 것보다 우리가 계산해서 주는 것이 강하다** — 날짜 산수가 그랬다
6. **UI가 불가능한 조합을 고르게 두고 한참 뒤에 실패시킨다**

---

## 작업 순서

### P1 — 엔진·모델 불일치 ✅ 완료 — 두 번 고쳤다

**1차(8e0e5d0)는 반쪽이었다.** 단위 테스트 9/9 를 근거로 ✅ 를 달았는데 새 빌드에서 같은
오류가 그대로 났다(라이브 4회째). 코드 통과는 검증이 아니다 — 이 하네스의 규칙을 내가 어겼다.

**진짜 뿌리(2차)**: 부팅이 `lastActiveUserId`(낡은 계정 acct1, claude-sonnet)를 먼저 복원하고
렌더러가 그 계정 값으로 라디오·hidden 을 그린다. 그 뒤 진짜 계정(openai-gpt41)으로 바뀌어도
**렌더러에 알리는 이벤트가 없다.** 화면은 옛 이름표 `claude` 를 보내고 main 은 새 계정 모델로
벤더를 검사해 던진다. 1차 수정(라디오가 SSOT)은 라디오 자체가 옛 계정 값이면 못 이긴다.
같은 부류 세 번째(06-30 에이전트 · 08-19 라디오 복원 · 오늘) — 화면 이름표를 고치는 수정은
늘 한 곳을 남겼다.

**2차 수정**: main.ts:5909 에서 이름표를 믿지 않는다. `primaryGeminiTextModel` 이 SSOT 고,
그 모델의 벤더와 이름표가 어긋나면 **모델은 그대로 두고 라벨만** 모델 쪽으로 맞춘다 + 경고.
사용자가 고른 모델이 그대로 도니 자동 폴백이 아니다. 에이전트 경로는 손대지 않는다.
(src/main/reconcileProviderWithModel.ts, 테스트 15건, 벤더 4조합 형태 확인)

**라이브 검증**: 재시작 후 부팅 직후 첫 생성이 claude 라벨로 들어와도 통과해야 한다 — 장부 행 추가.

**증상 (실측 3회, 오늘 하루)**
```
[ContentGenerator] ❌ TEXT_MODEL_PROVIDER_MISMATCH:
    expected=claude, selected=openai-gpt41, actual=openai
```
매번 **자료 수집을 끝낸 뒤** 실패한다. 직전 실패에서 29,215자를 크롤링하고
정제까지 마친 뒤 통째로 버렸다. 사장님은 원인을 알 수 없다.

**뿌리**
`primaryGeminiTextModel` 하나가 전 엔진 공용 모델 설정이다(이름만 Gemini).
값이 `openai-gpt41` 인데 엔진을 클로드로 고르면
`contentGenerator.ts:4705` 가 `resolveTextModelProfileForVendor(..., 'claude', ...)`
로 검사해 반드시 터진다. **엔진과 모델은 한 쌍인데 UI가 따로 고르게 둔다.**
자동 폴백 금지 규칙 때문에 가드는 정직하게 멈춘 것 — 가드는 제 일을 했다.

**고칠 방향** (둘 다 오늘 체크박스에서 쓴 방법)
1. 맞지 않는 엔진을 **UI에서 못 고르게** 하고 이유를 띄운다
2. 검사를 **수집 시작 전**으로 당긴다 — 실패할 거면 API 호출 전에 실패한다

**위험** 엔진 선택 UI를 못 찾으면 2번만 해도 값은 있다.
**검증** 엔진을 클로드로 두고 생성 → 수집 전에 멈추고 이유가 뜨는가

---

**3차 (2026-09-02 오후) — 같은 뿌리, 다른 자리**: 쇼핑커넥트 `SHOPPING_REFERENCE_MODEL_UNSUPPORTED`
"현재 OpenAI 이미지 모델: gpt-image-1.5" — 사장님은 2를 골랐고 그 값은 제대로 저장돼 있었다.
`settings_acct1.json`(부팅이 먼저 복원하는 낡은 계정) = 1.5, `settings_b52d….json`(진짜 계정) = 2.
imageManagementTab 이 부팅 때 낡은 계정 값을 localStorage 에 도장 찍고, costAndAutoGen 은 localStorage 를
먼저 읽는다. 08-25 · 09-01 두 수정은 "비어 있으면 config" — 빈 값만 막고 틀린 값은 못 막았다(P1 1차와 같은 실수).

**3차 수정**: `src/image/openaiImageModelReconcile.ts` — config 가 SSOT, 화면·localStorage·formData 는
config 가 빌 때만. 발화 경로 셋(costAndAutoGen · fullAutoFlow · main imageGenerator) 전부 지난다.
어긋나면 경고 찍고 config 로 맞춘다. 08-25 계약 테스트는 새 계약으로 갱신(옛 형태 박제 금지).

**아직 안 고친 것(뿌리 그 자체)**: 부팅 두 단계 계정 복원에서 렌더러에 "계정 바뀜" 알림이 없다.
P1 2차·3차는 전부 "읽는 순간 SSOT 로 맞추기" 다. 같은 부류가 넷째로 나오면 그때는 부팅 순서를 고친다.
### P2 — 주제 이탈 ✅ 완료 (라이브: 베란다 글 곁가지 8건 제외 · 화재·침수 소멸 · 총량 유지)

**증상 (실측 3편)**
| 글 | 본문 절반이 다룬 것 |
|---|---|
| 베란다 창문 | 실외기 화재 2,184건 · 지하주차장 침수 대피 |
| 침구 세탁 | 백화점 행사·매출 수치 두 섹션 |
| 입주청소 | 잠실 돔구장 · 양도소득세 · 대통령 발언 |

**뿌리 (41-agent 조사, 재현 확인됨)**
- `supplementTopicGuard.ts:119` — **토큰 2개만 맞으면 통과**.
  실외기 화재 기사도 `아파트`·`베란다` 가 있어 정상 통과한다. 게이트가 느슨하다.
- `sourceAssembler.ts:1619` — **키워드 모드에 비중 상한이 없다**.
  URL 모드는 `MAX_SUPPLEMENT_RATIO=3` 이 있는데 키워드 모드 두 수집기엔 대응물이 없다.

**고칠 방향** (조사가 "게이트를 조이지 말라" 고 판정)
게이트를 세게 만들면 자료가 마른다. **순서와 몫**으로 처리한다 —
주제어를 갖춘 자료(primary)를 먼저 쌓고, 나머지(secondary)는 총량의 30%까지만.
초과분은 버리고 건수만 로그로 남긴다. 발행은 막지 않는다.

**위험** 수집 파이프 한복판이다. 자료가 마르면 글이 앙상해진다.
반드시 자료 총량 로그를 전후 비교할 것.
**검증** 베란다 키워드 재생성 → 화재·침수 자료 비중이 줄고 총량은 유지되는가

---

### P3 — 감지기 사문화 ⛔ 무효화 (2026-09-02 실측 근거)

**적어둔 되돌림 장치는 발동할 경우가 없다.** 세 사실이 겹친다:
- `headingSkeletonVariety.findSharedPrefix` 는 `every` — **모든** 소제목이 같은 낱말로 시작해야 켜진다
- `MIN_HEADINGS = 3` — 소제목 3개 미만은 판정하지 않는다
- `resolveHeadingKeywordPatchMax` = 홈판 0 · 그 외 **2** — 우리 접두는 최대 2개에만 붙는다
3개 이상 중 최대 2개에 붙는 접두로는 "모두 같음" 이 성립하지 않는다.
실측의 `9월` 4연속은 `uniformNumberLead`(접두 2 + 모델 자체 2)였고, 그 뿌리는 a92f8e79d 가 상류에서 끊었다.

남는 것(DateClaim · FactVerify 가 로그만 남김)은 **재작성 금지 규칙에 따른 설계**이고,
그 감지기들이 잡던 것은 dateBasis · 상대 날짜 해석 · 크롤 정제로 상류에서 막았다.
→ 라이브 검증 장부에서 확인한다. 장부에서 다시 나타나면 그때 P3 를 다시 연다.

**증상** 감지기가 정확히 잡고도 글은 그대로 나갔다.
```
[DateClaim]      아직 오지 않은 날(9월 26일)을 이미 일어난 일처럼 썼습니다
[FactVerify]     검사한 주장 5건 중 확인 필요 5건
[HeadingVariety] 소제목 4개가 전부 숫자를 앞세웁니다
```

**뿌리** 대부분의 보수 경로가 `CONTENT_ALLOW_PAID_POST_GENERATION_REPAIR === '1'`
뒤에 있고 그 값은 프로덕션에서 설정된 적이 없다(코드 주석: "never set in production").

**고칠 방향** 재작성은 금지다. 대신 **우리가 만든 변형을 되돌리는 데** 쓴다 —
HeadingVariety 가 `sharedPrefix` 를 켰고 그 접두를 방금 우리가 붙였다면,
그것만 문자열로 되돌린다. 새 LLM 호출 0회.

**위험** SEO 롱테일 면이 줄 수 있다. 전부 같은 낱말로 시작할 때만 되돌린다.
**검증** 소제목이 전부 같은 접두일 때 그 접두가 사라지는가

---

### P4 — 설정 저장 경합 ✅ 완료

**증상** 기동 직후 `config:set` 이 한 번 실패한다.
```
Cannot read properties of null (reading 'configEncrypted')
  at migrateConfigToEncrypted (encryptionMigrator.js:31)
  at _saveConfigImpl (configManager.js:632)
```

**주의 — 흔한 수정이 더 나쁘다.**
`cachedConfig ?? {}` 로 막으면 바로 다음 줄 `writeFile` 이 **설정 파일을 `{}` 로 덮는다**.
API 키·네이버 계정이 전부 날아간다. 지금의 크래시가 그 쓰기 직전에 멈춰 방어 역할을 한다.
제대로 고치려면 **null 이면 저장을 명시적으로 중단**해야 한다.

**위험** API 키가 든 자리다. 별도 검증 필요.
**검증** 부팅 100회 중 설정 키 개수가 줄지 않는가

---

### P5 — 릴리즈 ⬅ 진행 중

**현재 v2.11.218 이후 30개 커밋이 사용자에게 안 갔다.** (2026-09-02 기준, 트리 깨끗)

**관문 — 아래 장부 행이 ✅ 되기 전에는 릴리즈하지 않는다.** 미검증 30개를 한 번에
내보내는 것은 "1릴리즈당 1~3 fix · cascade 금지" 위반이다. 코드 8,419 GREEN 은 검증이 아니다.
최소 관문(사장님이 앱에서 생성 → 글 붙여주면 내가 장부에 적는다):
- 침구 키워드(SEO) 1편 → 검색어 · 소제목 시점어 · 상대 날짜 · 남의 제목 인용 · 크롤 쓰레기 5행
- 건강 키워드(SEO) 1편 → 경험 재료 개방 1행
- 리빙(홈판) 1편 → 제목 감정→장면 1행
세 편이 나쁘지 않으면 릴리즈. 하나라도 나쁘면 그 행을 먼저 고친다.
오늘 고친 것 전부가 여기 들어 있다.

**순서** P1~P3 마친 뒤, 라이브 검증 장부가 채워지면 릴리즈.
`npm run release:full` 로만 한다(수동 분해 시 `latest.yml` 누락 → 자동업데이트 404).

---

## 라이브 검증 장부

코드 통과는 검증이 아니다. 실제 생성물에서 확인된 것만 `✅`.

| 커밋 | 고친 것 | 라이브 확인 |
|---|---|---|
| e8839279a | 검색어가 콜론 뒤 주제를 살린다 | ✅ `침구 구스 이불 차렵이불` |
| 15601f222 | 지식iN 질의 좁힘 | ✅ 0건 → 3건 |
| 7cdb27dc5 | 건강·쇼핑 경험 재료 개방 | ✅ category=건강 으로 지식iN 3건 (09-02) |
| 28a1c99e1 | 크롤 쓰레기 필터 | ✅ 4편 UUID·5G 없음 |
| ac03e3cff | 상대 날짜 해석 | ✅ "2026년 8월 28일부터 진행했습니다" |
| 1c0b5d5f5 | dateBasis 스키마 필드 | ⬜ |
| a92f8e79d | 소제목 시점어 배제 | ✅ 침구 글 소제목에 9월 없음 |
| b20f5a44e | 남의 제목 인용 금지 · 조합어 | ✅ 4편 재발 없음 (조합어 서술은 M3 로 별도) |
| 7c23e003a | 제목 감정→장면 | ✅ "아침엔 막히고 낮엔 덜할 때…" 상황형 |
| cad22fd93 | SERP 카드 가독성 | ⬜ |
| c2630f37b | 페르소나 교착 해소 | ⬜ |
| a3678c5e6 | 날조 의무화 4곳 | ⬜ |
| (P1 2차) | 엔진 라벨을 모델 벤더로 보정 | ⬜ 재시작 후 부팅 직후 첫 생성으로 확인 |
| (P1 3차) | 쇼핑커넥트 OpenAI 이미지 모델을 config 로 보정 | ⬜ 재시작 후 쇼핑커넥트 AI 이미지 생성 1회 — 경고 로그에 "어긋남" 이 찍히고 1.5 오류가 안 나야 |
| P9 | 에이전트 봉투에 자료 대조·빼거나 좁힘 | ⬜ 에이전트 모드로 1편 → 수치·날짜·기관명을 자료와 대조 |
| P10 | 쇼핑커넥트 OpenAI 참조 이미지 → edits multipart | ✅ 19:05 라이브 — 5/5 "edits 엔드포인트에 multipart" 후 생성 완료, 400 없음 |
| P11 | 수집 이미지 모달: 8장 그리드 + 대표 썸네일 + 클릭 뷰어 ‹ › | ⬜ 풀오토 쇼핑커넥트 모달에서 "8개" 배지·타일 8장·클릭 후 화살표 이동 |
| P12 | 프롬프트대로 생성: 타일이 자기 자리에 | ⬜ 이미지 관리 탭 "프롬프트대로 이미지 생성하기" — 1번만 바뀌지 않고 순서대로 채워져야 |
| P13 | 왼쪽 정렬 + 문단 2~3문장 기본 | ⬜ 새 글 1편 — 본문이 왼쪽 정렬이고, 문단이 2~3줄 뒤 빈 줄인지 |
| P14 | 요약 표 행이 "1. 5kg" 에서 두 동강 → 표 행 가드 | ⬜ 쇼핑 글 1편 — 표가 2열 모두 제 폭이고 "라벨 ㅡ 값" 문단이 표 밖에 없어야 |
| P15 | 쇼핑 모드 AI 경험 생성 — (a) 사용 장면 서술 | ⬜ 쇼핑 글 1편(체크박스 켬) — "구매자 의견이 있어요" 대신 현재형 사용 장면이 나오고, 내돈내산·사용 기간 주장은 없어야 |
| P16 | 문단 흐름 모드 — 문장이 이어지고 문단 사이만 빈 줄 | ⬜ 새 글 1편 — 문장마다 줄이 끊기지 않고 참고글처럼 2~3문장이 한 덩어리로 흐르는지 |
| P17 | 쇼핑 제목 게이트 — 후보 최고점 선택 · 구매 축 -30 · 옵션 조합 -20 | ⬜ 쇼핑 글 1편 — 로그에 "후보 중 최고점 선택" 이 찍히거나, 최종 제목에 상품 옵션 조합·상품명 중복이 없어야 |
| P18 | 쇼핑 제목 = 노출 우선 (검색어 그대로 앞쪽 · 상위 글 제목 대조) | ⬜ 쇼핑 글 1편 — 제목에 검색어 구절이 그대로 앞쪽에 있고, 벤치마크 카드에 "상위 N개 중 M개…" 제목 대조 줄이 뜨는지 |
| P18-2 | ① 앞 3자 강제 배선 ② 쇼핑 검색량 키워드 ③ 상위 제목 → 게이트 감점 | ⬜ 쇼핑 글 1편 — 로그에 [ShoppingKeyword] 교체/유지 · [SerpTitles] N개 확보 · [TitleDiag] 상위 글 제목 대조 가 찍히고, 제목이 검색어로 시작하는지 |
| P16-2 | 벽 방지 — 붙여넣기 흐름 모드도 2~3문장 묶음 | ⬜ 새 글 1편 — 편집을 거쳐도 벽 없이 2~3문장 문단 + 빈 줄 |
| P16-3 | 벽의 뿌리 — 주장 정제기 `\s{2,}` 가 문단 빈 줄을 지우던 것 | ⬜ 새 글 1편 — 편집 화면 본문에 문단 빈 줄이 그대로 보이고, 발행글도 2~3문장 문단 |
| P16-4 | 렌더러 번들 등록 누락 — `paragraphGroupSizes is not defined` 로 전체 자동화가 터진 것 + 같은 부류 잠복 4건 | ⬜ 새 글 1편 — 전체 자동화가 ReferenceError 없이 발행까지 가고, 시작 시 저장된 텍스트 모델 라디오가 복원된다 |
| P18-3 | 광고 API 키워드 정규화 · 스토어 꼬리표 제거 · 상품명 접두 생략 · 조기반환 제거 · off-keyword | ⬜ 쇼핑 글 1편 — [ShoppingKeyword] 교체/유지 가 11001 없이 찍히고, 제목에 [꼬리표]·상품명 통째가 없어야 |
| P19 | 후기 라디오 금지 · 하이라이트 한 문장 · 공정위 19px 굵게 가운데 빨강 | ⬜ 쇼핑 글 1편 — "구매자 후기에는…" 보고 문장 3개 이하, 형광펜이 한 문장, 공정위 문구 서식 |
| P19-2 | 공정위 서식이 문서 끝까지 번짐 — 마지막 문단 가운데 정렬 + 구분선·다음 글·해시태그 빨강 | ⬜ 새 글 1편 — 공정위 문구만 빨강·가운데, 마지막 문단은 좌측, 구분선·다음 글·해시태그는 본문색 |
| P20 | 쇼핑 글 1인칭 체험 — "후기가 이렇다" 중계가 아니라 작성자가 겪은 글 | ⬜ 새 글 1편 — "후기에서는/구매자들은" 문장 0, 본문이 "써보니/저는" 화자, 숫자 기간·내돈내산 없음, 로그 `[PromptBuilder] 🧭 AI 경험 옵트인: ON` |

**검증용 키워드 3종**
1. `9월 가을 환절기 침구 교체: 구스 이불 vs 차렵이불 세탁` (SEO) — 검색어·소제목·날짜·제목인용 동시 확인
2. `환절기 비염 코막힘 완화` (SEO, 건강) — 경험 재료 개방 확인
3. 아무 리빙 주제 (홈판) — 제목 감정→장면 확인

---

### P6 — 반자동 발행에 "비평 → 골라서 수정" 버튼 (사장님 제안 2026-09-02)

누르면 글을 분석·비평하고 개선점을 목록으로 보여준다. 체크한 것만 "수정하기" 로 고친다.

**왜 값이 있나**
사장님이 전에 거부한 두 가지와 다르다 —
"검사 후 재작성"은 *자동·전체·항상 비용*이었고, 이건 *수동·항목별·안 누르면 0원*이다.
"확인만 하면 뭐하냐"는 고칠 방법이 없어서였는데 이 버튼이 그것을 준다.
반자동은 어차피 사장님이 글을 보는 흐름이라 자리도 맞다.

**재료는 이미 다 있다** — 감지기가 잡고도 로그로 죽고 있다.
DateClaim · FactVerify · HeadingVariety · 경험 3요소 계약 · platitude 등.
새 감지기를 만드는 일이 아니라 갈 곳 없던 것들에 집을 주는 일이다.

**착수 조건 (P2~P4 완료 후)**
1. 파이프라인을 먼저 고칠수록 이 버튼이 고칠 거리가 준다.
   오늘 고친 것(소제목 접두 · 상대 날짜 · 크롤 쓰레기)은 애초에 안 생기므로 대상이 아니다.
2. **1차는 코드로 고칠 수 있는 것만** — 접두 제거 · 메타 문장 삭제 · 날짜 치환. 0원.
3. LLM 수정은 별도 옵트인. 안 그러면 사장님이 거부한 그 기능이 이름만 바꿔 돌아온다.

**주의** 이미 비슷한 것이 있다 — `🔁 품질 보정 패스`(기본 OFF, 2-pass, 자동).
그쪽은 자동·전체 재작성이라 이 버튼과 성격이 다르다. 중복 만들지 말고 관계를 정리할 것.

---

### P7 — 어미 믹싱 (사장님 지적 2026-09-02, 5번째 글) ✅ 완료 — 라이브 검증 대기

홈판 제목은 잘 나왔다. 문제는 **종결어미 혼용 / 어미 다양화**가 제대로 안 되는 것.
합니다/해요만 오가고 `~거든요 · ~잖아요 · ~죠` 같은 부연·동의 어미가 거의 없다.
게다가 멘토 톤이 "이거 하나만 알아도", "우리 모두", "여러분도 할 수 있어요" 같은
상투 동행 표현을 박는다.

**사장님 의도(그대로 명세로 쓴다)**
- "문어체와 구어체를 혼합해 리듬감을 주는 방식"
- "동일 어미 반복을 피해 자연스러운 호흡을 만드는 기법"
- "AI 특유의 균일한 어미 패턴을 깨는 어미 믹싱"

왜 사람 냄새가 나는가 — AI 는 한 문체(~합니다체)로 끝까지 밀고 가는데 사람은 무의식적으로
어미를 바꾼다. `~거든요 · ~잖아요 · ~죠` 는 부연·동의 유도 뉘앙스라 구어 리듬을 만들고,
`~했어요/~합니다` 를 섞으면 격식이 오르내리며 "생각하며 말하는" 느낌이 난다.

**진단부터** — promptLoader 톤별 어미 풀 · humanWritingAntiPattern 의 어미 조항 ·
멘토 톤의 동행 표현 강제("글당 3회+")를 읽고, 무엇이 균일함을 만드는지 찾는다.
나열된 어미 목록을 또 만들지 말 것(하드코딩 금지). 형태 규칙(연속 동일 어미 금지 · 부연 어미
비율)과 상투 동행 표현 제거가 방향이다.

### M — 내부 판정이 독자에게 서술되던 것 ✅ 완료

4편 중 3편 + 5번째 글에서 재확인. 세 갈래 한 뿌리:
- 자료 머리말의 출처 예시("블로그 사례에서는", "삼성 안내에는")를 그대로 베낌 — **내 커밋의 예시 은행**
- dateBasis 의 오늘 날짜·연도 판정을 문장으로 서술
- 조합어 표시문을 언급 대상으로 삼음
→ 예시 문구 제거 · "내부 판정, 서술 금지" 조항 · 표시문을 "언급·설명 금지" 로.
스니펫 경로에도 곁가지 몫 확장(④ 에버랜드 섹션).

---

### P8 — 쇼핑+라이프 카테고리 프롬프트 부재 (라이브 2026-09-02)

```
[PromptLoader] 프롬프트 파일 로드 실패: affiliate/life.prompt ENOENT
[ContentPolicy] 품질/정책 경고 후 이미지 단계 계속: BLOCK_KEYWORD_BODY_MISMATCH
```
`loadPromptFile` 은 ENOENT 에 `''` 를 돌려주는 **조용한 폴백**(promptLoader:148)이라
쇼핑 모드 라이프 카테고리는 카테고리 규격 없이 써진다. 그 글이 키워드-본문 불일치 경고를 받았다.
**방향**: (a) affiliate/life.prompt 를 만들거나 (b) 라이프를 있는 affiliate 카테고리로 매핑.
카테고리별 파일이 없는 조합을 전수로 세서 같은 구멍이 더 있는지 볼 것.

### P9 — 에이전트 모드 환각 (사장님 지시 2026-09-02: "에이전트모드로해도 할루시네이션없이 잘나와야되는데") ✅ 봉투 수정 완료 — 라이브 검증 대기

**판정(읽음)**: 에이전트 경로(agent-codex/claude/gemini)는 API 와 **같은 `prompt`** 를 받는다 —
`assembleContentSource` 가 provider 분기 앞(main.ts:5909)이라 자료·dateBasis 스키마·오늘 고친 것 전부 공유.
그 위에 `agenticEnvelope.wrapAsAgenticTask` 가 자율추론 봉투를 씌운다. 도구는 전부 차단(`--disallowedTools '*'`).

**구멍**: 봉투의 자기비평 항목 대부분이 문체(상투어·제목 틀·소제목 각도)고, 4단계 "수정" 에
"자료에 없는 사실을 새로 넣지 않는다" 가 없다. 검색을 못 하는 모델이 통과할 때까지 고쳐 쓰면
매 반복이 자료 밖 수치·날짜·기관명을 들일 기회다. 환각 점검은 SEO 포커스에 한 줄("팩트가 정확하고 추정·환각이 없는가")뿐.

**할 일(형태 규칙, 목록 아님)**:
1. 자기비평에 "본문의 수치·날짜·기관명·제품명이 전부 [작업 명세]의 자료 안에 있는가" — 모든 모드 공통
2. 수정 단계에 "고칠 때 자료에 없는 사실을 새로 넣지 않는다 — 근거가 없으면 빼거나 범위를 좁힌다"
3. 계약 테스트 + 봉투가 지문 닫힘 안이면 핀
4. 라이브: 에이전트 모드로 1편 → 자료 대조

**절대 규칙 적용**: 발행 차단 없음(봉투 문구만). 저비용 유지(구독 CLI, 추가 호출 0).

### P10 — 쇼핑커넥트 OpenAI 참조 이미지 400 (라이브 2026-09-02 16:45) ✅ 수정 완료 — 라이브 검증 대기

**증상**: `[OpenAI 이미지] ❌ 잘못된 요청입니다` — 5장 전부, 3회 재시도 모두 `status 400, code unknown_parameter: Unknown parameter: 'image'`.

**뿌리**: 2026-03-03 부터 참조 이미지를 `/v1/images/generations` 에 JSON `image` 필드로 보냈다. 그 엔드포인트는
image 를 모른다. 참조 이미지는 `/v1/images/edits` 에 multipart 로 간다. 쇼핑커넥트 img2img 는 그날 이후
한 번도 성공한 적이 없다 — 오늘 P1 3차로 모델 검사가 풀리자 그 뒤에 숨어 있던 400 이 드러난 것이다.

**수정**: `src/image/openaiImageEditsRequest.ts` — FormData(image 파일 파트 + model/prompt/n/size/quality). OpenAI 가
모르는 필드는 하나도 싣지 않는다(response_format·input_fidelity 제외). 생성기는 참조가 있을 때만 그 경로.

**모르는 것(정직하게)**: context7 이 이 세션에서 연결 실패라 API 문서를 실시간으로 대조하지 못했다. edits 의
필드 이름은 내가 아는 계약대로다. 라이브에서 다른 code 가 오면 그 code 를 그대로 보고하고 그 필드만 고친다.

### P11 — 풀오토 모달 수집 이미지 "1개" + preview unavailable (사장님 화면 2026-09-02) ✅ 수정 완료 — 라이브 검증 대기

**뿌리**: 수집 원소는 객체(`{url, filePath, heading…}`)인데 모달에 넘길 때 문자열로 취급해 `{url: <객체>}` 로
감쌌다. 주소가 전부 "[object Object]" → 중복 제거가 8→1 → 그 하나도 못 불러옴. 전체보기는 한 장짜리 오버레이라
화살표가 없었다(대표 미리보기+화살표 코드는 `return;` 뒤에 죽은 채 있었음 — 그 컨테이너 자체가 없다).

**수정**: `collectedImagePreview.ts`(형태 보고 감쌈, 첫 장=대표) + `ProgressImageViewer.ts`(타일 클릭 → 목록 전체를
받아 ‹ › · 화살표 키 · Esc). ProgressModal 은 위임만.

### P12 — 이미지 관리 탭 "프롬프트대로 생성" 1번 타일만 바뀜 (사장님 2026-09-02) ✅ 수정 완료 — 라이브 검증 대기

**뿌리**: 소제목마다 IPC 한 번 → main 은 늘 `index 0 / total 1` → 브리지가 `updateSingleImage(0, …)` → 1번 타일이
생성된 이미지를 하나씩 이어받았다. IPC 는 루프의 자리를 모른다.

**수정**: `imagePreviewBatch.ts` — 루프가 begin/slot/end 로 자리를 알려주고 브리지가 한 장짜리 이벤트만 그 자리로 옮긴다.
여러 장 이벤트는 손대지 않는다. 끝내지 못한 낡은 배치는 무시한다.

### P13 — 좌측 정렬 + 문단 2~3줄 기본값 (사장님 결정 2026-09-02) ✅ 수정 완료 — 라이브 검증 대기

**결정**: "문단정리 중앙정렬 말고 좌측정렬로. 문단정리도 기본값에서 2~3줄에서 줄바꿈 띄우기로."
어제 4편이 전부 문장 하나 = 문단 하나로 나온 것에서 출발.

**뿌리(셋)**: ① 에디터 진입 직후 가운데 정렬(5/27 요청, editorHelpers) ② 후처리기 `enforceSentenceParagraphs` 가
마침표마다 빈 줄(9/1 쇼핑글 벽 지시) — 모든 모드 ③ 홈판 프롬프트·골격 "문단 1~2문장(실측 35~50자)" + 긴 본문 안전망이 1~3 랜덤.

**수정**: ① 왼쪽 정렬 셀렉터 추가, 진입 단계가 왼쪽을 누름(화면 모드·이미지 가운데 정렬은 그대로)
② 3문장 초과 문단만 2~3문장씩 고르게 나눔(4→2+2, 5→3+2). 3 이하·기존 빈 줄은 손대지 않음(합치지 않음).
③ 홈판 base·issue-story·골격 "2~3문장(모바일 2~3줄)", 안전망 2~3. 실측(40편) 수치는 기록으로만 남김 — 사장님 결정이 기본값.

**절대 규칙 적용**: 문단 안 문장별 줄바꿈(v2.10.393b 사장님 요청)은 유지 — 2~3줄 뒤 빈 줄이라는 뜻과 맞는다.

**P13 보완(같은 날 저녁)**: 에디터 정렬 버튼만 바꿔서는 부족했다 — 붙여넣기 HTML 이 문단마다 `text-align:center` 를
박는다(richTextPaste `centerAlign` 기본 true, 넘기는 호출처 없음). 닥터웰 발행글이 여전히 가운데였던 이유. 기본을 왼쪽으로.
소제목 상자·표 셀·목차 장식은 그대로.

### P14 — 요약 표 깨짐 (사장님 화면 2026-09-02, 닥터웰) ✅ 수정 완료 — 라이브 검증 대기

**증상**: 표의 "규격" 행이 셀 1개(폭 100%)라 1열이 통째로 밀리고 2열이 한 글자 폭으로 눌림(PC 화면). 전원·출시연월·KC
인증정보 행은 "라벨 ㅡ 값" 문단으로 표 밖에 찍힘. "1. 5kg" 표기.

**진단**: 발행 HTML 을 직접 읽어 셀 수 확인(2,2,2,1). dist 로 재현 — 문단 후처리·옵티마이저·옛 문장분리기 전부 무관,
`buildMobileRichHtml` 단독 재현. 뿌리는 인라인 번호 목록 확장기(`normalizeInlineNumberedLists`)가 행 안의 "1. 5kg" 을
목록으로 보고 앞에 빈 줄을 넣어 행을 두 동강 낸 것. 잘린 앞쪽이 셀 1개 행, 뒤쪽 행들은 표 블록 밖 → orphan-pipe 문장.

**수정**: 표 행(파이프 시작 줄)은 번호·대시·Q/A 어떤 인라인 확장기도 건드리지 않는다. 요약 표 값의 "숫자. 숫자" 는
소수점으로 붙인다(표는 문장을 담지 않는다). 재현 프로브: 4 경로 전부 7행·전부 2셀·새는 문단 0.

### P15 — 쇼핑 모드에서 AI 경험 생성 (사장님 요청 2026-09-02 저녁) ✅ (a) 로 수정 완료 — 라이브 검증 대기

**요청**: "구매자 의견은 후기를 직접 보면 보이니까, 없는 실사용 정보를 사람들이 원한다. AI 경험 생성이 가능하니까 이것도."

**판정(읽음)**: 체크박스(`aiExperienceGeneration`)는 affiliate 에서도 켜지고 `shared/experience-contract.prompt` 오버레이가
붙는다(contentGenerator 2721). 그런데 쇼핑 전용 계층이 더 강하다 — AFFILIATE AUTHENTICITY CONTRACT 의 evidenceMode
(FIRST_PARTY/REVIEW_SYNTHESIS/SPEC_ONLY, 스키마 필드)와 shopping_review·spec_analysis·expert_review 프롬프트가
"FIRST_PARTY(사용자 경험 메모)일 때만 1인칭, 아니면 써보니·실사용·내돈내산 금지" 를 못 박는다. 그래서 켜도
"구매자 의견이 있어요" 로 나온다. 두 계층이 충돌하고 구체 계층이 이긴다.

**제안(형태 규칙)**: 옵트인이 켜졌고 경험 메모가 없으면, evidenceMode 는 그대로 두고 "사용 장면 서술" 을 허용한다 —
스펙·리뷰에서 도출한 현재형 1인칭 장면("착용하면 발끝부터 차오른다", "누워서 쓰면 소음이 거슬리지 않는다").
구매 사실·사용 기간·내돈내산처럼 검증 불가능한 주장은 계속 금지. 수치·기관명은 자료 안에서만.

**결정 필요(사장님)**: (a) 사용 장면 서술까지 — 추천 / (b) 구매·기간 주장까지 허용. (b)는 표시광고법상 허위 체험 위험이
사장님 몫이라 제가 임의로 고르지 않는다.

**결정(사장님, 같은 날 저녁)**: (a). 계약(affiliateAuthenticity)과 리뷰 부재 가드(reviewGuard) 둘 다에 같은 문구의 옵트인 블록을 넣었다 —
현재형·조건형 사용 장면 허용, 과거 체험 주장·구매·기간·내돈내산·가족 반응 금지 유지. FIRST_PARTY(경험 메모) 는 옵트인 블록을 얹지 않는다.

### P16 — 문단 흐름 모드 (사장님 참고글 2026-09-02 밤: "이런식으로 원하는데?") ✅ 수정 완료 — 라이브 검증 대기

**참고글의 형태**: 문단 안에서 문장이 이어지고 줄은 화면 폭에 맞춰 자연스럽게 접힘. 2~3문장이 한 덩어리, 문단 사이만 빈 줄, 왼쪽 정렬.
(참고글도 모바일에서 낱말 중간에서 접힌다 — 그것을 감수한 선택이다. 6/11·8/22 의 "22자 하드 줄바꿈" 요청을 이 참고글이 대체한다.)

**우리가 달랐던 것**: 붙여넣기가 문장마다 `<p>` 를 만들고 22자마다 `<br>` 을 박았다(모바일 폭 보정). 변환 계층도 문단 안
문장마다 줄바꿈(v2.10.393b). 그래서 데스크톱에서 짧은 줄이 들쭉날쭉했다(닥터웰 화면).

**수정(세 계층 같은 규칙)**: ① richTextPaste 흐름 모드 기본(`flowParagraphs`, 한 줄 = `<p>` 하나, 22자 끊기·꼬리 보정 없음)
② contentBodyTransforms 문단 안 문장 이어 쓰기(문장별 줄바꿈 함수는 호출처 유지하고 무동작) ③ sentenceParagraphs 가 빈 줄 안의
홑 줄바꿈 평문을 먼저 잇고 2~3문장씩 묶음. 옛 청킹은 `flowParagraphs:false` 로만 남겨 테스트가 두 길을 다 잠근다.

**남은 것(사장님 결정 필요)**: 참고글의 소제목 스타일(큰 따옴표 + 굵은 글씨 + 구분선). 지금은 색 상자 소제목. 원하시면 다음 항목.

### P16-4 — 번들 등록 누락 (사장님 로그 2026-09-03: `ReferenceError: paragraphGroupSizes is not defined`) ✅ 수정 완료 — 라이브 미검증

**증상** — 전체 자동화가 문단 정리 직전에 `paragraphGroupSizes is not defined` 로 죽는다. tsc·lint·build 는 전부 통과했다.

**뿌리** — 렌더러는 단일 스코프 인라인 번들(`scripts/copy-static.mjs`)이라, 렌더러 밖 모듈을 import 하면 그 파일을 등록 목록에 손으로 넣어야 한다. `richTextPaste` 가 새로 import 한 `content/sentenceParagraphs` 가 빠져 있었다. 빌드는 이런 누락을 잡지 못한다 — 런타임에서만 터진다.

**같은 부류를 가드 테스트로 전수 조사** (`rendererBundleImportRegistry.test.ts` 신설) → 이미 잠복해 있던 등록 누락 3건이 더 나왔다. 번들에 정의 없이 호출만 있던 것들:
- `runtime/modelRegistry` (엔진 라벨·가격 모달·이미지 서사 퀵모드가 `isAgentTextProvider`/`resolveTextModelProfile` 호출)
- `freeTrialPolicy` (라이선스 UI 가 `FREE_TRIAL_DAILY_PUBLISH_LIMIT` 참조)
- `scheduler/scheduledPostLookupPolicy` (예약 관리가 `selectScheduledPostCandidate` 호출)

**등록하자 드러난 둘째 뿌리** — modelRegistry 는 `export { X } from './y.js'` 재수출을 쓰는데, tsc 는 이걸 `var y_js_2 = require(...)` + `Object.defineProperty(exports, …getter)` 로 낸다. 의존 파일 정제기(`scripts/rendererRuntimeDependencyInline.mjs`)는 `const` require 와 `_js_1` 접두만 알아서 그대로 새어 나왔고, 그 상태로 앱을 띄웠다면 렌더러가 통째로 죽었을 것이다(번들 검증에서 잡아 재시작 전에 막음). 정제기를 `const|var|let` · `_js_N` · 재수출 getter 제거로 일반화했다.

**덤으로 잡힌 기존 잠복 버그** — `renderer.ts` 가 `settingsModal` 을 두 import 문으로 들여 `settingsModal_js_2.restoreTextModelRadio` 가 번들에 남아 있었다(try/catch 안이라 조용히 실패). 시작 시 저장된 텍스트 모델 라디오 복원이 그동안 실행되지 않았다. `_js_N` 일반화로 함께 해소.

**충돌 1건** — 렌더러 로컬 헬퍼 `normalizeImageProvider`(표시용 trim) 가 modelRegistry 의 같은 이름(딥인프라 별칭 접기) 과 단일 스코프에서 겹쳐 빌드 중복 스캐너가 막았다 → 로컬 쪽을 `normalizeImageProviderLabel` 로 개명.

**셋째 뿌리 — 조립 순서(사장님 2차 보고: `Cannot access 'GEMINI_TEXT_MODELS' before initialization`)** — copy-static 은 utils(의존 파일 포함) → runtime 순으로 앞에 붙이는데(prepend), 나중에 붙일수록 번들 앞으로 간다. 결과적으로 runtime 모듈이 의존 파일보다 **뒤**에 놓여, modelRegistry 의 상위 `const VISION_MODELS = { …GEMINI_TEXT_MODELS… }` 가 선언 전 접근(TDZ)으로 죽었다. runtime 두 모듈은 import 가 0 이라 최상단이 맞다 → 붙이는 순서를 바꿔 runtime 이 번들 최상단에 오게 했다. 가드 테스트에 순서 잠금 추가.

**검증** — 번들: 정의 8종 각 1개, `exports`/`require("./`/`_js_N.` 잔재 0, `node --check` OK. 정제기 잠금 테스트(재수출 getter·var require·`_js_2`) 추가.

### P19-2 — 공정위 서식 번짐 (사장님 화면 2026-09-03: "선이랑 다음글도 궁금하다면이랑 해시태그는 왜 빨간색", "마지막 문단은 왜 중앙정렬") ✅ 수정 완료 — 라이브 미검증

**뿌리** — P19 에서 공정위 문구를 `<p style="text-align:center;font-size:19px;font-weight:700;color:#ff0000">` 로 붙이고 Enter 두 번을 눌렀다. 스마트에디터는 Enter 로 만드는 새 문단에 캐럿 문단의 정렬·글자 서식을 물려준다. 그 빈 빨간 가운데 문단이 문서 맨 끝에 남아, 마지막 섹션 붙여넣기가 거기 합쳐지며 가운데 정렬이 되고(글자색은 span 이 이겨 본문색), 그 뒤 키보드로 치는 구분선·다음 글·해시태그는 통째로 빨갛게 나왔다. 2026-08-06 형광펜 번짐과 같은 부류다.

**수정** — ① 정렬만 문단에, 19px·굵게·빨강은 span 에만 둔다. ② 공정위 문구 뒤에 본문 기본값(좌측·15px·400·본문색) 리셋 문단을 같은 붙여넣기로 붙여 캐럿이 거기서 이어진다(`buildTypingStyleResetHtml`). ③ 꼬리 타이핑(구분선 앞)에서도 리셋 문단을 한 번 더 붙인다(`pasteTypingStyleReset`).

### P20 — 쇼핑 글 1인칭 체험 (사장님 2026-09-03: "1인칭시점에서 글이나와야지 … 후기가이렇다 이런식으로 넣지말라니까") ✅ 수정 완료 — 라이브 미검증

**뿌리** — 3인칭 중계는 한 군데가 아니라 다섯 층에 못 박혀 있었다: ① 진정성 계약(REVIEW_SYNTHESIS: "작성자 체험처럼 바꾸지 않는다", 옵트인(a) 블록도 "현재형·조건형만"), ② 제목 지시("구매자 후기에서 확인된" 제목 권장), ③ 리뷰 부재 가드, ④ 평가기·감사(1인칭 = FABRICATED_FIRST_PERSON 하드 실패 → 퇴고 루프가 3인칭으로 되돌림, 출처 표기 없음 = MISSING_REVIEW_ATTRIBUTION), ⑤ V3 프롬프트·프롬프트 로더·전환 골격("구매자 의견으로 귀속", "2인칭 시뮬레이션"). 게다가 옵트인 체크박스 기본값이 꺼짐이라 켜지 않으면 (a) 조차 적용되지 않았다.

**수정** — 옵트인이 켜지면 다섯 층 전부 1인칭 체험으로 전환: 후기·스펙의 사실을 작성자가 겪은 것처럼 쓴다("써보니/저는/제 경우엔", 과거형 허용). "후기에서는/구매자들은/리뷰에 따르면" 중계 문장은 금지이고 감사에서 `REVIEW_RELAY_VOICE` 로 잡는다. 지어내면 안 되는 것은 그대로: 숫자 기간(`UNSUPPORTED_DURATION_CLAIM` — "써보니"는 되고 "한 달 써보니"는 안 됨), 구매 시점, 내돈내산, 가족 반응, 자료에 없는 수치·기관. 체크박스 기본값 켬. 매 생성마다 로그 `[PromptBuilder] 🧭 AI 경험 옵트인: ON/OFF`.

**사장님 실제 발행글 실측(224399398683 "누가 이걸 구매하고싶어하냐")** — 옛 감사는 이 글을 90/100 으로 통과시켰다. 세 가지가 새고 있었다: ① "구매자 반응에는/한 구매자는/~다고 남겼어요" 중계 어투(출처 표기 패턴보다 넓음), ② 면책 해설 "이 변화는 해당 구매자의 경험이며 제품 전체의 결과로 묶어 말할 수는 없어요"·"해석할 근거는 없으므로"(프롬프트에만 있던 후기 평가 금지가 새어 나옴), ③ KC 인증번호·제조국·약관 위치 나열. 감사에 `REVIEW_RELAY_VOICE`(옵트인) · `EVIDENCE_META_NARRATED`(모든 모드) · `SPEC_DUMP`(옵트인) 를 추가해 같은 글이 이제 85 미만으로 떨어져 퇴고 루프에 걸리고, 옵트인 계약·V3·로더에 "결론은 단정한다, ~수 있어요/~긴 해요 는 문단당 하나" 를 못 박았다.

**리스크(사장님 결정)** — 실사용 없이 1인칭으로 쓰는 글이다. 공정위 문구는 그대로 붙고 숫자 기간·구매 선언은 막지만, 체험의 외피는 사장님 책임이다.

### P17 — 쇼핑 제목이 팔리는 제목이 아니던 것 (사장님 2026-09-02 밤: "문제는 팔려야 되잖아") ✅ 1차 수정 — 라이브 검증 대기

**19:05 닥터웰 실측**: 모델 후보 3개 중 2번("닥터웰 에어웨이브 DR-5180 공기압 마사지기, 종아리 압박 위치 후기")이 멀쩡했는데,
최종 게이트는 selectedTitle(1번: 상품명 두 번 + 옵션 "그레이 본체+다리" + "설명서 소음 후기", 58자, 25점)만 보고
중복만 지워 95점으로 통과시켰다. 후보는 채점조차 안 했다. 구매 축 없음 감점이 -10 이라 상품명 나열도 95점이 됐다.

**수정(형태 규칙)**: ① 게이트는 고쳐 쓰기 전에 후보를 같은 잣대로 채점해 최고점(≥50, 현재보다 높을 때)을 고른다
② 쇼핑 구매 축 없음 -10 → -30 ③ "A+B" 옵션 조합 표기 -20 (낱말 목록 아님 — 형태만 본다).
실측 두 제목으로 잠금: 후보 2번 70점 > 고쳐 쓴 1번 50점.

**남은 것**: 스토어 상품명 자체(SEO 범벅 "닥터웰 종아리 공기압 마사지기 발 장화 안마기 에어웨이…")가 모델 입력으로 그대로 가서
제목 후보가 거기서 출발한다. 핵심 상품명(브랜드+품목+모델코드) 추출은 다음 항목. 제목 채점기가 "팔리는 제목" 을 직접 재지는 못한다 —
구매 축·검색어 맞물림·중복·옵션·길이 형태만 본다. 클릭이 실제로 오르는지는 SERP 장부가 판정한다.

### P18 — 쇼핑 제목은 노출이 먼저 (사장님 2026-09-02 밤) ✅ 1차·2차 수정 — 라이브 검증 대기

**사장님 사슬**: 팔린다 → 고객이 내 글을 보고 판단 → 보려면 노출 → 노출은 SEO·황금키워드 → 그러니 먼저 상위노출될 제목.
"제휴마케팅 할 때 사람들이 제목으로 정하는 걸 참고해서 대량으로 학습하고 내 거랑 대조."

**앱에 대입한 진단**:
① 키워드 — URL 모드는 모델 후보 중 검색량 최대를 고른다(광고 키 있을 때, urlModeKeywordPicker). 쇼핑 크롤 경로는
  스토어 상품명이 그대로 keywords[0] 이 된다(sourceAssembler). 황금키워드 자동선정(블루오션)은 SEO 한정.
② 제목 — 앞 3자 강제(`ensureFront3`)는 코드·단위 테스트만 있고 **부르는 곳이 없어 죽어 있다**(SPEC-KEYWORD-ENDGAME Phase 1).
  맞물림 채점(scoreSearchMatch)은 낱말 단위라 흩어져 있어도 만점. 쇼핑 제목 지시에 검색어 위치가 없었다.
③ 대조 — 프로브(serp:benchmark)가 상위 글 제목을 이미 가져오는데 본문 점수만 비교했다.

**1차 수정(형태 규칙)**: ⓐ 쇼핑 제목 지시 — "검색어를 그대로 붙여서 제목 앞쪽에, 그 뒤에 구매 상황" ⓑ 쇼핑 채점 —
검색어 구절 토막 -10, 뒤로 밀림 -4(`scoreSearchPhraseIntact`) ⓒ 벤치마크 — 상위 글 제목과 내 제목을 구절 온전성·앞쪽 배치·
길이로 대조해 뒤처지면 첫 번째 고칠 점, 맞으면 강점으로(`serpTitleBenchmark`). 추가 호출 0 — 이미 가져온 제목을 쓴다.


**2차(사장님 승인 "승인해줄게 다 하고 테스트해보게")**:
① 앞 3자 강제 — 마지막 키워드 접두 단계에 `ensureFront3: isSearchDrivenTitleMode(mode)` 를 실제로 넘긴다(seo·mate·affiliate·business).
  홈판은 그대로. 이미 선두면 무변경, 아니면 재배치 — 제목 훼손 여부는 라이브 확인.
② 쇼핑 검색량 키워드 — 상위호환 분석이 정한 키워드(keywordOrigin=upgrade-analysis)일 때만, 사람들이 치는 상품 검색어
  후보(품목형·브랜드+품목형, 머리 명사 필수)를 받아 검색량으로 재검토. 검색량이 확실히 더 클 때만 교체, 못 구하면 유지.
  **사용자가 넣은 키워드는 건드리지 않는다**(기존 계약 `if (existing) return;` 유지 — 회귀에서 잡혀 고침).
③ 상위 글 제목 → 게이트 — 생성 단계에서 제목만 먼저 받아(fetchSerpTitles, 본문 없음) source 에 싣고, 동기 게이트가 후보마다
  대조해 상위 다수와 어긋나면 -8. 가산 없음. 실패하면 대조 없이 진행.

**남은 것(다음)**: 스토어 크롤 경로(keywords=[상품명, 스토어명])는 아직 검색량 재검토 대상이 아니다 — 출처 표시가 없어서. 다음 항목.

### P16-2 — 벽 (사장님 화면 2026-09-03 새벽: "문단정리 하나도 안되어있는데..??") ✅ 수정 완료 — 라이브 검증 대기

**증상**: 발행글이 빈 줄 없는 벽. 생성 단계는 45문장 → 15문단으로 나눴는데(로그), 편집 경로(_bodyManuallyEdited=true, bodyText 3094자)를
거친 본문이 빈 줄 없는 한 줄로 붙여넣기에 왔고(청크 668자 → "모바일 단락 1개"), 흐름 모드는 한 줄을 문단 하나로 만들었다.

**수정**: 붙여넣기 흐름 모드에도 같은 규칙 — 한 줄에 문장이 3개를 넘으면 2~3문장씩 묶어 문단을 나누고 문단마다 빈 문단. 어떤 입력이 와도 벽은 없다.
**뿌리(찾음, 2026-09-03 오전)**: 편집 경로가 아니었다. 생성 꼬리에서 `ensureContentParagraphBreaks` 가 45문장을 15문단으로 나눈
직후 `sanitizeStructuredContentClaims`(공식 가이드 주장 정제기)가 돌며 `.replace(/\s{2,}/g, " ")` 로 문단 사이 빈 줄을 공백 하나로
뭉갰다 — 본문·도입·결론·소제목 내용 전부. 실측 프로브: 빈 줄 2 → 0. 렌더러 정리·풀오토 재절단·에디터 헬퍼는 전부 빈 줄을 보존한다
(각각 실측). 가로 공백만 접도록 고쳤다(`[ 	]{2,}`). 회귀 테스트로 잠금. 같은 부류(\s 로 본문 접기)는 다른 본문 변환에 없었다.

### P18-3 — 헬스헬퍼 맥스컷 실측으로 드러난 제목 파괴 (2026-09-03 새벽) ✅ 수정 완료 — 라이브 검증 대기

**로그**: ① 검색량 조회 5건 전부 `11001 hintKeywords 파라미터가 유효하지 않습니다` → 키워드 유지 ② 상위 10개 중 0개가 검색어를 그대로 씀
→ 대조기가 "aligned" 로 오독 ③ 후보 3개 전부 "헬스헬퍼 맥스컷 프로 크롬 [슈퍼적립+사은품 증정] 헬스헬퍼 맥스컷"(37자 동일 머리)
→ 리뷰형 상품명 접두 단계가 모델 제목 앞에 스토어 상품명(꼬리표 포함 32자)을 붙이고 45자로 잘라 모델 제목이 13자만 남았다.

**수정**: 광고 API 키워드 공백 제거(기존 지표 수집과 같은 규칙) · 대조기 off-keyword 판정(검색어가 검색어가 아닐 때 제목 탓 안 함, 감점 없음) ·
스토어 꼬리표([…]) 제거를 상품명 원천·게이트 복구·채점 세 곳에 · 쇼핑은 상품명 접두 단계를 건너뛴다(검색어 앞 배치가 대신) ·
상품명≈키워드 조기 반환 제거(게이트가 늘 돈다).

### P19 — 후기 라디오 · 통째 하이라이트 · 공정위 서식 (사장님 2026-09-03 새벽) ✅ 수정 완료 — 라이브 검증 대기

**후기 라디오**: REVIEW_SYNTHESIS(후기 12건)가 "구매자 후기에는… 또 다른 구매자는… 의견도 이어집니다" 로 이어졌다. 옵트인은 꺼져 있었음.
계약이 "필요한 곳에서만 출처를 밝힌다" 로 열어 둔 자리를 모델이 문장마다 썼다. → 한 건씩 읽어 주지 않는다, 반복 의견은 사실로 압축해
장면·판단으로(작성자 체험 위장은 여전히 금지), 출처 표기는 갈리는 자리에서 한 번, 글 전체 세 개 이하.
**하이라이트**: 흐름 모드에서 문단(2~3문장, 벽이면 더)이 통째로 칠해졌다 → 문단 안 최고 문장만(선택 규칙은 그대로, 따옴표 인용 1순위).
**공정위 문구**: 타이핑이라 그 순간 서식을 따라갔다 → HTML 붙여넣기로 19px·굵게·가운데·#ff0000 못 박음. 실패하면 타이핑으로.
