# 🚀 배포 전 체크리스트

## ⚠️ 필수 보안 조치

### 1. API 키 초기화
```bash
node prepare-for-release.js
```

또는 수동으로:

**src/renderer/renderer.ts (85-95번 줄)**
```javascript
const apiKeys = {
  'openai-api-key': '',  // ← 빈 문자열로 변경
  'gemini-api-key': '',  // ← 빈 문자열로 변경
  'claude-api-key': '',  // ← 빈 문자열로 변경
  'pexels-api-key': ''   // ← 빈 문자열로 변경
};
```

**test-apis.js (7-12번 줄)**
```javascript
const API_KEYS = {
  gemini: '',   // ← 빈 문자열로 변경
  openai: '',   // ← 빈 문자열로 변경
  claude: '',   // ← 빈 문자열로 변경
  pexels: ''    // ← 빈 문자열로 변경
};
```

### 2. 네이버 계정 정보 확인
- 저장된 설정 파일 삭제:
  - `%APPDATA%\better-life-naver\config.json`
  - `~/.better-life-naver/config.json`

### 3. 테스트 파일 제외
배포 시 다음 파일들을 제외하세요:
- `test-apis.js`
- `test-api-keys.html`
- `unified-tab-new.html`
- `prepare-for-release.js`
- `*.backup*`
- `*.bak`

## 📦 배포 명령어

### 개발용 빌드 (테스트)
```bash
npm run build
npm start
```

### 배포용 패키징
```bash
# 1. API 키 초기화
node prepare-for-release.js

# 2. 빌드
npm run build

# 3. 패키징
npm run pack    # 또는 npm run dist
```

## ✅ 배포 후 복원

배포 완료 후 개발 환경으로 복원:

```bash
# Git으로 원본 파일 복원
git checkout src/renderer/renderer.ts
git checkout test-apis.js

# 또는 백업에서 복원
# (백업 파일이 있는 경우)
```

## 🔒 보안 권장사항

1. **API 키는 절대 하드코딩하지 마세요**
   - 환경 변수 사용 권장
   - 사용자가 직접 입력하도록 유도

2. **네이버 계정 정보**
   - 앱에서 사용자가 직접 입력
   - "계정 정보 기억하기" 옵션 제공

3. **민감한 정보 제외**
   - 테스트 파일
   - 백업 파일
   - 로그 파일
   - 설정 파일

## 📝 배포 전 최종 확인

- [ ] API 키 초기화 완료
- [ ] 테스트 파일 제외 확인
- [ ] 백업 파일 제외 확인
- [ ] 설정 파일 삭제 확인
- [ ] 빌드 오류 없음 확인
- [ ] 패키징 성공 확인
- [ ] 실행 파일 테스트 완료

## 🎯 배포 후

배포 완료 후 **반드시** 개발 환경 복원:
```bash
git checkout src/renderer/renderer.ts
```

이렇게 하면 다음 개발 시 API 키를 다시 입력할 필요가 없습니다.


## ⚠️ 필수 보안 조치

### 1. API 키 초기화
```bash
node prepare-for-release.js
```

또는 수동으로:

**src/renderer/renderer.ts (85-95번 줄)**
```javascript
const apiKeys = {
  'openai-api-key': '',  // ← 빈 문자열로 변경
  'gemini-api-key': '',  // ← 빈 문자열로 변경
  'claude-api-key': '',  // ← 빈 문자열로 변경
  'pexels-api-key': ''   // ← 빈 문자열로 변경
};
```

**test-apis.js (7-12번 줄)**
```javascript
const API_KEYS = {
  gemini: '',   // ← 빈 문자열로 변경
  openai: '',   // ← 빈 문자열로 변경
  claude: '',   // ← 빈 문자열로 변경
  pexels: ''    // ← 빈 문자열로 변경
};
```

### 2. 네이버 계정 정보 확인
- 저장된 설정 파일 삭제:
  - `%APPDATA%\better-life-naver\config.json`
  - `~/.better-life-naver/config.json`

### 3. 테스트 파일 제외
배포 시 다음 파일들을 제외하세요:
- `test-apis.js`
- `test-api-keys.html`
- `unified-tab-new.html`
- `prepare-for-release.js`
- `*.backup*`
- `*.bak`

## 📦 배포 명령어

### 개발용 빌드 (테스트)
```bash
npm run build
npm start
```

### 배포용 패키징
```bash
# 1. API 키 초기화
node prepare-for-release.js

# 2. 빌드
npm run build

# 3. 패키징
npm run pack    # 또는 npm run dist
```

## ✅ 배포 후 복원

배포 완료 후 개발 환경으로 복원:

```bash
# Git으로 원본 파일 복원
git checkout src/renderer/renderer.ts
git checkout test-apis.js

# 또는 백업에서 복원
# (백업 파일이 있는 경우)
```

## 🔒 보안 권장사항

1. **API 키는 절대 하드코딩하지 마세요**
   - 환경 변수 사용 권장
   - 사용자가 직접 입력하도록 유도

2. **네이버 계정 정보**
   - 앱에서 사용자가 직접 입력
   - "계정 정보 기억하기" 옵션 제공

3. **민감한 정보 제외**
   - 테스트 파일
   - 백업 파일
   - 로그 파일
   - 설정 파일

## 📝 배포 전 최종 확인

- [ ] API 키 초기화 완료
- [ ] 테스트 파일 제외 확인
- [ ] 백업 파일 제외 확인
- [ ] 설정 파일 삭제 확인
- [ ] 빌드 오류 없음 확인
- [ ] 패키징 성공 확인
- [ ] 실행 파일 테스트 완료

## 🎯 배포 후

배포 완료 후 **반드시** 개발 환경 복원:
```bash
git checkout src/renderer/renderer.ts
```

이렇게 하면 다음 개발 시 API 키를 다시 입력할 필요가 없습니다.


## ⚠️ 필수 보안 조치

### 1. API 키 초기화
```bash
node prepare-for-release.js
```

또는 수동으로:

**src/renderer/renderer.ts (85-95번 줄)**
```javascript
const apiKeys = {
  'openai-api-key': '',  // ← 빈 문자열로 변경
  'gemini-api-key': '',  // ← 빈 문자열로 변경
  'claude-api-key': '',  // ← 빈 문자열로 변경
  'pexels-api-key': ''   // ← 빈 문자열로 변경
};
```

**test-apis.js (7-12번 줄)**
```javascript
const API_KEYS = {
  gemini: '',   // ← 빈 문자열로 변경
  openai: '',   // ← 빈 문자열로 변경
  claude: '',   // ← 빈 문자열로 변경
  pexels: ''    // ← 빈 문자열로 변경
};
```

### 2. 네이버 계정 정보 확인
- 저장된 설정 파일 삭제:
  - `%APPDATA%\better-life-naver\config.json`
  - `~/.better-life-naver/config.json`

### 3. 테스트 파일 제외
배포 시 다음 파일들을 제외하세요:
- `test-apis.js`
- `test-api-keys.html`
- `unified-tab-new.html`
- `prepare-for-release.js`
- `*.backup*`
- `*.bak`

## 📦 배포 명령어

### 개발용 빌드 (테스트)
```bash
npm run build
npm start
```

### 배포용 패키징
```bash
# 1. API 키 초기화
node prepare-for-release.js

# 2. 빌드
npm run build

# 3. 패키징
npm run pack    # 또는 npm run dist
```

## ✅ 배포 후 복원

배포 완료 후 개발 환경으로 복원:

```bash
# Git으로 원본 파일 복원
git checkout src/renderer/renderer.ts
git checkout test-apis.js

# 또는 백업에서 복원
# (백업 파일이 있는 경우)
```

## 🔒 보안 권장사항

1. **API 키는 절대 하드코딩하지 마세요**
   - 환경 변수 사용 권장
   - 사용자가 직접 입력하도록 유도

2. **네이버 계정 정보**
   - 앱에서 사용자가 직접 입력
   - "계정 정보 기억하기" 옵션 제공

3. **민감한 정보 제외**
   - 테스트 파일
   - 백업 파일
   - 로그 파일
   - 설정 파일

## 📝 배포 전 최종 확인

- [ ] API 키 초기화 완료
- [ ] 테스트 파일 제외 확인
- [ ] 백업 파일 제외 확인
- [ ] 설정 파일 삭제 확인
- [ ] 빌드 오류 없음 확인
- [ ] 패키징 성공 확인
- [ ] 실행 파일 테스트 완료

## 🎯 배포 후

배포 완료 후 **반드시** 개발 환경 복원:
```bash
git checkout src/renderer/renderer.ts
```

이렇게 하면 다음 개발 시 API 키를 다시 입력할 필요가 없습니다.












