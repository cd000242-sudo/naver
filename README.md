# Better Life Naver - 네이버 블로그 자동화

더 나은 삶을 위한 AI 기반 블로그 자동화 도구

콘텐츠 생성부터 자동 포스팅까지, 네이버 블로그 관리를 한 번에 처리하는 Electron 데스크톱 애플리케이션입니다.

[![CI](https://github.com/cd000242-sudo/naver/actions/workflows/ci.yml/badge.svg)](https://github.com/cd000242-sudo/naver/actions/workflows/ci.yml)
[![Latest release](https://img.shields.io/github/v/release/cd000242-sudo/naver)](https://github.com/cd000242-sudo/naver/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/cd000242-sudo/naver/total)](https://github.com/cd000242-sudo/naver/releases)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.x-3178C6?logo=typescript&logoColor=white)](https://www.typescriptlang.org/)

## 프로젝트 현황

- **유지관리 상태**: 활발히 유지관리 중
- **주요 기술**: TypeScript, Electron, Puppeteer, Playwright, Vitest
- **배포 대상**: Windows 및 macOS 데스크톱 앱
- **품질 관리**: CI, 릴리스 검증 게이트, 자동화 테스트, 보안 회귀 테스트
- **최신 버전**: [GitHub Releases](https://github.com/cd000242-sudo/naver/releases/latest)에서 확인

기여 방법은 [CONTRIBUTING.md](CONTRIBUTING.md), 보안 취약점 제보 방법은 [SECURITY.md](SECURITY.md)를 확인해 주세요.

## 주요 기능

### 🤖 AI 콘텐츠 생성
- **구조화 콘텐츠 자동 생성**: RSS 피드, URL, 키워드, 초안 텍스트를 입력하면 AI가 제목, 소제목, 본문, 해시태그를 자동으로 생성합니다.
- **다중 AI 엔진 지원**: Gemini, Claude, OpenAI 중 선택 가능
- **소제목 기반 구조화**: 본문을 논리적인 소제목 단위로 자동 분할하여 가독성 높은 포스팅 생성

### 🎨 이미지 자동 생성 및 관리
- **OpenAI 이미지 생성**: GPT 이미지 모델을 사용해 소제목별 이미지를 자동 생성
- **Pexels 이미지 라이브러리**: 키워드 기반으로 무료 이미지 자동 수집
- **이미지 라이브러리**: RSS/URL 크롤링 시 수집된 이미지를 카테고리별로 관리
- **이미지 자동 배치**: 소제목 아래에 이미지를 자동으로 삽입

### 📝 스마트 포스팅
- **글만 발행하기**: 이미 생성된 콘텐츠를 수정 없이 바로 발행
- **자동 생성만 실행**: 콘텐츠만 생성하고 발행은 하지 않음
- **발행 모드**: 임시저장, 즉시발행, 예약발행 지원
- **CTA 링크**: 포스팅 하단에 자동으로 링크 삽입

### 📊 품질 분석
- **AI 탐지 위험도**: 생성된 콘텐츠의 AI 탐지 가능성 분석
- **법적 위험도**: 저작권 및 법적 문제 가능성 체크
- **SEO 점수**: 검색 엔진 최적화 점수 제공
- **일일 발행 제한**: 하루 권장 발행 횟수 관리

## 플랫폼 지원

- **Windows**: ✅ 완전 지원 (Windows 10/11)
- **macOS**: ✅ 지원 (Intel/Apple Silicon 모두 지원)

현재 공식 배포 패키지는 Windows와 macOS를 대상으로 합니다.

## 준비 사항

- **운영체제**: Windows 10+, macOS 10.15+, Ubuntu 18.04+
- **Node.js 18 이상**
- **npm** 또는 다른 Node 패키지 매니저
- **네이버 계정** (아이디 / 비밀번호)
- **API 키** (선택 사항):
  - Gemini API Key (Google AI Studio)
  - OpenAI API Key (OpenAI 이미지 생성용)
  - Pexels API Key (이미지 라이브러리용)
  - Claude API Key (Anthropic, 선택 사항)
- **저장 공간**: Puppeteer가 Chromium을 다운로드하므로 수백 MB 필요

## 설치

```bash
# 저장소 클론 (또는 압축 해제)
cd "리더 네이버 자동화"

# 의존성 설치
npm install
```

> `puppeteer`가 설치되면서 Chromium이 자동으로 다운로드됩니다. 이미 설치된 Chromium/Chrome을 사용하려면 [`PUPPETEER_EXECUTABLE_PATH`](https://pptr.dev/guides/configuration) 환경변수를 설정하세요.

## 실행

### 데스크톱 앱 실행

```bash
npm start
```

- `npm start`는 TypeScript를 `dist/`로 빌드하고, Electron 앱을 실행합니다.
- 앱이 실행되면 UI에서 모든 작업을 수행할 수 있습니다.

> 개발 중 빠른 확인이 필요하면 `npm run build` 후 `npm run dev`로 바로 Electron을 실행할 수 있습니다. (코드 변경 시 다시 `npm run build` 필요)

### CLI 실행 (디버깅용)

```bash
npm run cli
```

환경 변수(`NAVER_ID`, `NAVER_PASSWORD`)나 `.env`에 계정 정보를 제공해야 합니다.

## 환경 설정

### 환경 변수 파일 (`.env`)

프로젝트 루트에 `.env` 파일을 생성하여 기본 설정을 저장할 수 있습니다:

```ini
# 네이버 계정 (선택)
NAVER_ID=your-id
NAVER_PASSWORD=your-password

# AI API 키 (선택)
GEMINI_API_KEY=your-google-ai-key
OPENAI_API_KEY=your-openai-key
PEXELS_API_KEY=your-pexels-key
CLAUDE_API_KEY=your-claude-key

# Gemini 모델 선택 (기본값: gemini-1.5-flash)
# GEMINI_MODEL=gemini-1.5-flash
```

> ⚠️ **보안 주의**: `.env` 파일은 버전 관리에서 제외되어야 합니다. 민감한 정보를 포함하므로 공유하지 마세요.

### 앱 내 환경 설정

앱 실행 후 상단의 **환경 설정** 섹션에서 API 키를 입력하고 저장할 수 있습니다. 입력한 키는 사용자 데이터 폴더에 저장됩니다.

## 배포 및 빌드

### 플랫폼별 빌드

```bash
# Windows 전용 빌드
npm run dist:win

# macOS 전용 빌드 (Intel/Apple Silicon 모두 지원)
npm run dist:mac

# macOS Universal 빌드 (Intel + Apple Silicon)
npm run dist:universal

# 모든 플랫폼 빌드
npm run dist
```

### 빌드 결과

빌드 완료 후 `release_final/` 폴더에 설치 파일이 생성됩니다:
- **Windows**: `.exe` 설치 파일
- **macOS**: `.dmg` 설치 파일

### macOS에서 실행 시 주의사항

macOS에서 처음 실행 시 보안 경고가 나타날 수 있습니다:

1. **시스템 설정 > 보안 및 개인 정보 보호 > 일반** 탭으로 이동
2. "어썬가"에서 Better Life Naver 앱을 허용
3. 앱을 다시 실행

## 사용 가이드

### 1. 기본 포스팅 (수동 입력)

1. **네이버 계정 입력**: 네이버 아이디와 비밀번호를 입력합니다.
2. **제목 및 본문 작성**: 제목과 본문을 직접 입력합니다.
3. **해시태그 추가** (선택): 해시태그를 입력합니다.
4. **발행 모드 선택**: 임시저장, 즉시발행, 예약발행 중 선택합니다.
5. **자동화 실행**: **자동화 실행** 버튼을 클릭합니다.

### 2. AI 구조화 콘텐츠 생성

#### 방법 1: RSS 피드 사용
1. **RSS URL 입력**: RSS 피드 URL을 입력합니다.
2. **AI 엔진 선택**: Gemini, Claude, OpenAI 중 선택합니다.
3. **자동 생성만 실행**: **자동 생성만 실행** 버튼을 클릭합니다.
4. **결과 확인**: 생성된 제목, 소제목, 본문을 확인하고 수정합니다.
5. **이미지 생성** (선택): 소제목별로 이미지를 생성합니다.
6. **발행**: **자동화 실행** 또는 **글만 발행하기** 버튼을 클릭합니다.

#### 방법 2: URL 크롤링
1. **URL 입력**: 크롤링할 웹페이지 URL을 입력합니다.
2. **AI 엔진 선택**: 원하는 AI 엔진을 선택합니다.
3. **자동 생성만 실행**: 콘텐츠를 생성합니다.
4. **결과 확인 및 수정**: 생성된 콘텐츠를 검토하고 필요시 수정합니다.
5. **발행**: 발행 버튼을 클릭합니다.

#### 방법 3: 키워드 기반 생성
1. **키워드 입력**: 콘텐츠와 관련된 키워드를 입력합니다.
2. **초안 텍스트** (선택): 참고할 초안 텍스트를 입력합니다.
3. **AI 엔진 선택**: 원하는 AI 엔진을 선택합니다.
4. **자동 생성만 실행**: 콘텐츠를 생성합니다.
5. **결과 확인 및 발행**: 생성된 콘텐츠를 확인하고 발행합니다.

### 3. 이미지 생성 및 관리

#### 이미지 자동 생성 (OpenAI Image)
1. **구조화 콘텐츠 생성**: 먼저 구조화 콘텐츠를 생성합니다.
2. **소제목 선택**: 이미지 탭에서 이미지를 생성할 소제목을 선택합니다 (최대 10개).
3. **프롬프트 수정** (선택): 각 소제목에 대한 이미지 프롬프트를 수정할 수 있습니다.
4. **이미지 생성**: **이미지 자동 생성** 버튼을 클릭합니다.
5. **결과 확인**: 생성된 이미지를 확인하고 필요시 다시 생성할 수 있습니다.

> ⚠️ **주의**: OpenAI 이미지 모델은 콘텐츠 정책에 위배되는 프롬프트를 거부할 수 있습니다. 프롬프트를 수정하여 재시도하세요.

#### 이미지 라이브러리 (Pexels)
1. **이미지 탭 이동**: 이미지 관리 탭으로 이동합니다.
2. **키워드 입력**: 검색할 키워드를 입력합니다.
3. **이미지 수집**: **이미지 수집** 버튼을 클릭합니다.
4. **카테고리 관리**: 수집된 이미지를 카테고리별로 관리합니다.
5. **이미지 사용**: 발행 시 자동으로 사용되거나 수동으로 선택할 수 있습니다.

### 4. 글만 발행하기

이미 생성된 콘텐츠를 수정 없이 바로 발행하고 싶을 때 사용합니다.

1. **콘텐츠 확인**: 제목, 본문, 소제목이 모두 준비되었는지 확인합니다.
2. **글만 발행하기**: **글만 발행하기** 버튼을 클릭합니다.
3. **이미지 제외**: 이 모드에서는 이미지가 삽입되지 않고 텍스트만 발행됩니다.

> 💡 **팁**: 이미 생성된 콘텐츠를 수정한 후 다시 발행할 때 유용합니다.

### 5. 발행 모드

#### 임시저장 (기본값)
- 네이버 블로그에 임시저장으로 저장됩니다.
- 나중에 수동으로 발행할 수 있습니다.

#### 즉시발행
- 생성된 콘텐츠를 즉시 발행합니다.
- 발행 후 바로 블로그에 노출됩니다.

#### 예약발행
1. **예약발행 선택**: 발행 모드에서 "예약발행"을 선택합니다.
2. **날짜/시간 입력**: 예약할 날짜와 시간을 입력합니다.
3. **자동화 실행**: 지정된 시간에 자동으로 발행됩니다.

### 6. CTA 링크 추가

포스팅 하단에 링크를 자동으로 삽입할 수 있습니다.

1. **CTA 링크 입력**: 링크 URL을 입력합니다 (예: `https://example.com`).
2. **CTA 텍스트 입력**: 링크에 표시될 텍스트를 입력합니다 (예: "더 알아보기", "구매하기").
3. **자동화 실행**: 발행 시 자동으로 하단에 삽입됩니다.

### 7. 품질 분석

구조화 콘텐츠가 생성되면 자동으로 다음 지표가 표시됩니다:

- **AI 탐지**: AI로 생성된 콘텐츠로 탐지될 가능성
- **법적 위험**: 저작권 및 법적 문제 가능성
- **SEO 점수**: 검색 엔진 최적화 점수
- **일일 권장**: 하루 권장 발행 횟수 (설정에서 변경 가능)

## 타이핑 순서

자동화 실행 시 콘텐츠는 다음 순서로 입력됩니다:

1. **제목 입력**
2. **소제목 입력** (인용구 없이 일반 텍스트)
3. **Enter 키 입력**
4. **본문 영역 클릭**
5. **이미지 삽입** (생성된 이미지가 있는 경우)
6. **Enter 키 입력**
7. **정렬 설정** (가운데 정렬)
8. **본문 내용 입력**
9. **해시태그 입력**
10. **CTA 링크 삽입** (있는 경우)
11. **발행 모드에 따라 저장/발행**

> **글만 발행하기** 모드에서는 이미지 삽입이 건너뛰어지고 텍스트만 입력됩니다.

## 콘텐츠 정책 파이프라인

모든 구조화 글 생성과 실제 발행은 다음 순서를 거칩니다.

`InputValidator → SearchIntentAnalyzer → TopicDiversifier → OutlineGenerator → DraftGenerator → SimilarityGuard → QualityGate → PublishGuard → ExposureMonitor`

- 기본 정책 파일: `config/content_policy.yaml`
- 최근 게시물 비교: 최소 20건, 권장 50건
- 품질 통과 점수: 85점
- 유사도 또는 품질 실패 시 최대 2회 구조 재설계 후 `BLOCK`
- 최근 게시물 저장소가 없거나 손상됐거나 20건 미만이면 자동 통과시키지 않고 수동 검수 상태로 보냅니다.
- 즉시발행, 반자동, 풀오토, 연속발행, 다중계정, 앱 예약발행, SmartScheduler가 모두 동일한 `PublishGuard`를 거칩니다.
- 다중계정은 글·이미지 API를 호출하기 전에 `PAUSED`, 발행 간격, 일일 상한을 먼저 검사합니다.
- 네이버 서버 예약 성공은 공개 완료로 기록하지 않습니다. 구체적인 `/blogId/logNo` URL이 확인된 즉시발행만 노출 추적 큐에 등록됩니다.

### 발행 제한 설정

```ini
# 0 또는 미설정이면 해당 제한을 사용하지 않습니다.
MIN_PUBLISH_INTERVAL_MINUTES=30
DAILY_PUBLISH_CAP=5

# 노출 모니터는 기본 활성입니다. 점검 작업에서만 false로 끕니다.
EXPOSURE_POLLER_ENABLED=true
EXPOSURE_POLLER_INTERVAL_HOURS=6
```

값이 숫자가 아니거나 음수이면 안전을 위해 발행을 차단합니다. 노출 확인은 발행 후 24시간, 48시간, 72시간 시점에 URL 접근, 정확한 제목 검색, 네이버 블로그 검색 탭, 통합검색을 교차 확인합니다.

### 중단과 재개

앱의 `분석 도구 → 콘텐츠 정책`에서 상태와 감사 기록을 확인합니다.

1. `자동발행 중단`을 누르고 사유를 입력하면 전체 상태가 `PAUSED`로 바뀝니다.
2. 첫 `MISSING_CONFIRMED`는 같은 템플릿과 구조를 중단하고, 연속 2건이면 전체 자동발행을 중단합니다.
3. 누락 원인분석 기록을 확인하고, 수정한 테스트 글 1건을 수동으로 발행합니다.
4. 현재 누락 사건과 같은 블로그·기본 키워드의 테스트 글 URL, 정확한 제목, 검색 키워드를 입력해 `노출 검증`을 실행합니다.
5. URL과 검색 노출의 독립 검증 증적이 저장된 뒤 승인자, `원인 검토`, `수동 테스트 확인`을 입력해야 재개됩니다.

현재 중단 시각보다 오래된 원인분석, 다른 블로그·키워드의 테스트 글, 체크박스 값만 전송한 요청, 감사로그·노출 증적이 없는 요청은 재개되지 않습니다. 자동 삭제와 즉시 재발행은 수행하지 않습니다.

### 정책 데이터

사용자 데이터 폴더에 다음 파일이 자동 생성됩니다. 별도 DB 마이그레이션은 없습니다.

- `content-policy-articles.json`: 제목, 도입부, 소제목, 본문, 주제 각도, 구조, 실제 사실, 질문, 발행시각, 노출상태
- `content-policy-audit.jsonl`: 입력 해시, 검색의도, 유사도, 품질점수, 판정, 모델·프롬프트·정책 버전, 노출 확인과 원인분석
- `content-policy-publication-state.json`: 전체/템플릿/구조 중단 상태, 발행 이력, 수동 테스트 증적, 재개 승인
- `published-posts.json`: 24/48/72시간 노출 폴링 큐

기존 로컬 글 저장소에서 완전한 발행 글을 가져와 초기 이력을 구성합니다. 신규 설치처럼 비교 가능한 글이 20건 미만인 환경은 정책상 자동 생성·발행을 시작하지 않으며, 과거 게시물 데이터를 보강하거나 수동 검수를 거쳐야 합니다.

### 회귀 방지 게이트

- `npm run verify:release`는 린트, 전체 테스트, 프로덕션 빌드를 순서대로 실행합니다.
- Windows/macOS 패키징과 GitHub 릴리스 경로는 이 게이트를 통과하지 못하면 중단됩니다.
- 글 생성 취소는 요청별 ID로 분리되며, 로그의 `[Generation:<id>]`와 `[CancelTrace]`로 취소 주체를 추적할 수 있습니다.
- 서로 다른 글 생성 요청은 동시에 실행되지 않습니다. 먼저 시작한 작업이 끝나거나 정확히 취소된 뒤 다음 요청을 받습니다.
- Flow는 동시 제출을 사용하지 않고 프롬프트마다 독립 브라우저 컨텍스트에서 순차 생성합니다. 성공·지연·타임아웃 여부와 관계없이 이전 요청의 늦은 이미지가 다음 소제목에 붙을 통로를 차단합니다.

네이버 예약 API가 공개 후 글 번호를 즉시 반환하지 않는 예약 글은 예약 시점에 노출 모니터링 대상으로 확정할 수 없습니다. 이 경우 예약 자체는 품질·발행 정책 검사를 통과해야 하지만, URL 기반 24/48/72시간 노출 추적은 구체적인 공개 URL이 확보된 글에만 적용됩니다.

## 문제 해결

### 이미지가 생성되지 않을 때
- **API 키 확인**: OpenAI API Key가 올바르게 설정되었는지 확인하세요.
- **프롬프트 수정**: 콘텐츠 정책에 위배되는 프롬프트인지 확인하고 수정하세요.
- **재시도**: 이미지 생성 실패 시 프롬프트를 수정하여 다시 시도하세요.

### 본문 영역 클릭 실패
- **대기 시간**: 네이버 블로그 에디터가 완전히 로드될 때까지 기다려주세요.
- **수동 확인**: 브라우저 창에서 수동으로 본문 영역을 클릭한 후 다시 시도하세요.
- **로그 확인**: 로그 패널에서 상세한 오류 메시지를 확인하세요.

### AI 콘텐츠 생성 실패
- **API 키 확인**: 해당 AI 엔진의 API Key가 올바르게 설정되었는지 확인하세요.
- **인터넷 연결**: 인터넷 연결 상태를 확인하세요.
- **API 할당량**: API 사용량 제한에 도달했는지 확인하세요.

### 정렬 버튼을 찾을 수 없을 때
- **자동 처리**: 정렬 버튼을 찾지 못해도 본문 작성은 계속 진행됩니다.
- **수동 정렬**: 발행 후 네이버 블로그에서 수동으로 정렬을 설정할 수 있습니다.

## 프로젝트 구조

```
리더 네이버 자동화/
├── src/
│   ├── main.ts                    # Electron 메인 프로세스
│   ├── preload.ts                 # IPC 브릿지
│   ├── naverBlogAutomation.ts     # Puppeteer 자동화 핵심 로직
│   ├── contentGenerator.ts        # AI 콘텐츠 생성
│   ├── imageGenerator.ts          # 이미지 생성 공급자 통합
│   ├── imageLibrary.ts            # Pexels 이미지 라이브러리
│   ├── gemini.ts                  # Gemini API 통합
│   ├── configManager.ts           # 설정 관리
│   ├── renderer/
│   │   ├── renderer.ts            # UI 로직
│   │   └── global.d.ts            # TypeScript 타입 정의
│   └── ...
├── public/
│   ├── index.html                 # UI HTML
│   └── styles.css                 # 스타일시트
├── dist/                           # 빌드 출력 디렉토리
├── package.json
├── tsconfig.json
└── README.md
```

## 주요 파일 설명

- **`src/main.ts`**: Electron 메인 프로세스. 창 생성, IPC 처리, 자동화 실행 관리.
- **`src/preload.ts`**: Renderer 프로세스에 안전한 IPC 브릿지를 노출.
- **`src/renderer/renderer.ts`**: UI 이벤트 처리 및 사용자 인터랙션 관리.
- **`src/naverBlogAutomation.ts`**: Puppeteer를 사용한 네이버 블로그 자동화 핵심 로직.
- **`src/contentGenerator.ts`**: AI 엔진을 사용한 구조화 콘텐츠 생성.
- **`src/imageGenerator.ts`**: OpenAI Image 등 이미지 생성 공급자를 통합.
- **`src/imageLibrary.ts`**: Pexels API를 사용한 이미지 수집 및 관리.

## 기존 파이썬 스크립트

`naver_blog_automation.py`는 참고용으로 남겨 두었습니다. TypeScript 기반 앱으로 완전히 전환이 끝났다면 필요에 따라 삭제하거나 별도로 보관하세요.

## 기여 및 보안

- 버그 수정, 테스트, 문서 개선 제안은 [기여 가이드](CONTRIBUTING.md)를 따라 제출해 주세요.
- 보안 취약점은 공개 이슈에 세부 내용을 올리지 말고 [보안 정책](SECURITY.md)의 비공개 제보 절차를 이용해 주세요.

## 라이선스

이 프로젝트에서 직접 작성한 코드는 별도 표기가 없는 한 [GNU Affero General Public License v3.0 이상](LICENSE)에 따라 제공됩니다. AGPL은 상업적 이용도 허용하지만, 수정·배포하거나 수정본을 네트워크 서비스로 제공할 때에는 해당 라이선스의 소스 공개 의무를 준수해야 합니다.

프로젝트가 필요한 권리를 보유한 코드에 대해서는 AGPL 의무를 원하지 않는 조직과 별도의 상용 라이선스를 협의할 수 있습니다. 적용 범위와 제3자 구성요소에 관한 자세한 내용은 [라이선스 안내](docs/LICENSING.md), [상용 라이선스 안내](COMMERCIAL-LICENSING.md), [제3자 고지](THIRD_PARTY_NOTICES.md)를 확인해 주세요.

앱의 **라이선스 인증** 기능은 배포된 애플리케이션의 이용 권한을 확인하기 위한 기능이며, 소스 코드 라이선스와는 별개입니다.

## 지원

문제가 발생하거나 질문이 있으시면:
1. 로그 패널에서 상세한 오류 메시지를 확인하세요.
2. 이 README의 문제 해결 섹션을 참고하세요.
3. 필요시 개발자에게 문의하세요.

---

- **현재 버전**: [최신 릴리스 보기](https://github.com/cd000242-sudo/naver/releases/latest)
- **유지관리 상태**: Active
