/**
 * Source-review approval data. This file is intentionally excluded from the
 * candidate/evaluation runtime fingerprint so adding an approved evidence
 * digest does not create a self-referential runtime hash.
 */
export const APPROVED_CONTENT_QUALITY_V3_EVIDENCE_ARTIFACT_SHA256: readonly string[] =
  Object.freeze([]);
