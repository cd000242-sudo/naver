# SPEC-BLUEPRINT-2026 — 조사 (2026-09-04)

## 질문
"애초에 글 자체를 수정할 필요 없게 생성하려면 어떻게 해야 하나." (사장님, 2026-09-04 02:00)
전제: 이 툴의 가치는 사람 손 없이 홈판·상위노출급 글을 자동으로 내는 것. "내보낼 만한 글"은 기준이 아니다.

## 오늘 실측에서 나온 사실

### 1. 모델은 규칙은 흘리고 재료는 쓴다
| 요구 | 규칙으로 시켰을 때 | 재료·구체 지시로 줬을 때 |
|---|---|---|
| 직접 인용 | 2회 생성 모두 0건 (GPT-5.6 Terra) | 재료 발언 5개를 지시문에 넣자 3건 (같은 엔진) |
| 도입부 독자 상황 | Gemini 3.6 · GPT-5.6 · Claude Code 전부 분석형 예고문 | 패치에 "고칠 점"을 적어 주자 1회에 "…헷갈릴 때예요" |
| 자료 목록 서술 금지 | 프롬프트에 금지 예시 있어도 gpt-4.1 이 재발 | finalize 에서 문장 제거(materialNarrationStrip) |

### 2. 프롬프트는 65,723자, 재료가 절반 가까이
- 홈판 GPT 호출 프롬프트 65,723자(에이전트 봉투 포함 68,647자). 그중 재료(사실 자료 블록) 24~30K자, 규칙 약 40K자.
- 재료는 원문 통째 주입이라 곁가지가 그대로 들어간다: 청년도약계좌 글에 청년미래적금 통계 더미, 전세 글에 장기전세주택 퇴거 통계.
- 큰 프롬프트에서 작은 규칙(첫 문장 형태, 인용, 목록 서술 금지)이 묻힌다. 엔진을 올려도(3.6 flash 58점 → GPT-5.6 77점) 같은 지적이 났다 → 파이프라인 문제.

### 3. 게이트는 거의 매번 발화한다
- 홈판 GPT-5.6 검증 4회 중 4회 재생성 또는 patch. 재생성 1회 + 자기비평 1회 = 본문 호출 2회 + 소보정. 비용·시간이 두 배.
- 그중 2회는 검사기 오탐(천원 표기·연도)이 안전 점수를 35로 끌어내려 부른 것 — 오늘 수정. 남은 오탐: 감정 키워드 누락 휴리스틱(정치 의혹 글에 "진심·진정성 누락"을 환각 신호로).

### 4. 출력 스키마에 있는데 아무도 안 쓰는 필드
- `readerSituation`(독자가 이 글을 만나는 상황) — contentJsonPromptFormat 이 요구하고 모델이 채우지만 src 어디서도 소비하지 않는다. 도입부와 묶여 있지 않아 모델이 채워 놓고도 도입부는 예고문으로 쓴다.
- `evidence`(근거 인용) — 같은 상태.

### 5. 이미 있는 자산
- URL 모드 1단 분석(`paraphraseSourceAnalysis`): clickReason · mainKeyword · subKeywords · skeleton · evidenceAnchors · gaps 를 JSON 으로 뽑고 본문 프롬프트에 브리프로 붙인다. "분석 → 집필" 2단이 이미 한 경로에 있다.
- 에이전트 봉투(`agenticEnvelope`): 분석 → 초안 → 자기비평 → 수정을 CLI 안에서 돈다. API 엔진에는 이 단계가 없다.
- `quoteCoverage.extractDirectQuotes`(재료 발언 추출), `materialTierNotice`(재료 급 표시), `resolveSelectedEngineRoute`(보조 호출도 고른 엔진), 게이트·재생성·패치(오늘 기본 ON).

## 제약 (변경 불가)
- 자동 폴백 금지(엔진을 몰래 바꾸지 않는다). 발행 차단 게이트 신설 금지(경고만). 하드코딩 금지, 사용자 키 원칙.
- god file(contentGenerator.ts) 계약 테스트: finalize 반환 라인 핀, V3 wiring, 지문 핀, 레거시 베이스라인(프롬프트·평가기 바이트).
- 파일 300줄 상한, 테스트 우선, 1릴리즈 1~3 fix(단계 분할).
- 비용: 통과 글 추가 비용 0 유지. 미달 글 재생성 1회 상한 유지.

## 결론
규칙을 더 쓰는 방향은 한계다. 본문 호출 전에 **짧은 설계도**(독자 상황·인용 후보·근거 사실·골격·제외 주제)를 뽑아 그것을 재료로 주고, 스키마 필드를 조립 코드가 실제로 소비하게 묶어야 첫 호출에서 형태가 잡힌다. 게이트는 보험으로 남긴다.

## 6. 비용 실측 (2026-09-04 02:26, 홈판 1편, GPT-5.6 Terra, OPENAI_DIAGNOSTICS=1)
| 호출 | 입력 토큰 | 출력 토큰 | 비용 |
|---|---|---|---|
| 본문 1차 (규칙 33,440자 + 재료 16,504자) | 28,476 | 4,481 | $0.138 |
| 도입부 패치 (규칙 21,254자 통째 + 72자) | 11,809 | 431 | $0.036 |
| 본문 2차 (Faithfulness 재생성) | 28,741 | 5,182 | $0.150 |
| 도입부 패치 2 | 11,860 | 86 | $0.031 |
| **합계** | **80,886** | **10,180** | **$0.355** (게이트 87/92 통과) |

새는 곳: ① 입력이 절반 — 규칙부에 글마다 다른 조각이 섞여 캐시 적중이 불확실(캐시 시 입력 단가 1/10). ② 출력의 43%가 메타(요약·키워드·이미지 프롬프트·제목 후보 설명·해시태그) — 출력 단가는 입력의 6배. ③ 소보정이 규칙 21K자를 통째로 다시 보냄. ④ 재생성 1회 = +$0.15.


### 6.4 캐시 미적중 원인 실측 (09-04 02:43, 기준선 배치 1~3편)

| 편 | 1차 본문 호출 cached | 재시도 호출 cached | system 길이 |
|---|---|---|---|
| 홈판 1 | 0 / 33,402 | 18,639 / 33,851 | 33,408자 |
| 홈판 2 | 0 / 34,242 | 18,608 / 34,733 | 33,359자 |
| 홈판 3 | 0 / 35,697 | (재시도 없음) | 33,352자 |

- 같은 편 안의 재시도는 system 부분(≈18.6K 토큰)이 캐시에 맞는다. **편과 편 사이는 0.**
- 원인: `[수집 자료에서 지켜야 할 사실 30개]` 블록(키워드 모드 사실 목록)이 system 프롬프트 **맨 앞에 prepend** 된다(contentGenerator 6255~6262). 접두가 편마다 다르면 OpenAI 프롬프트 캐시는 그 뒤 33K 전체를 버린다. URL 지시·적응 학습·페르소나 카드도 같은 자리에 붙는다.
- 레버(Phase 2.5): 편별 블록은 `[원본 텍스트]` 마커 뒤(user 파트)로 옮기고, 설계도가 사실 목록 역할을 대신한다. 기대치는 적지 않는다 — Phase 4 에서 cachedTokens 로 실측.

### 6.5 편마다 강제되던 유료 재생성의 뿌리 (09-04 02:45, 기준선 1~4편 4/4)

로그: `Faithfulness 실패 — 재시도(attempt 0): 인용 밀도 0.00 < 0.3 (총 0/1단락)` ×4.

- PlatitudeDetector 의 인용 밀도·배치 기준은 본문 속 `[자료N]` 토큰을 센다. 그런데 본문 프롬프트는 전 모드에서 그 토큰 노출을 금지한다(seo/base F2, shared/exposure-structure). 토큰 수는 계약상 항상 0 → 기준은 항상 실패.
- 어제(09-03)까지는 유료 보정 경로가 꺼져 있어 경고만 남았다. 켜자마자 이 실패가 **통과 불가능한 재생성**으로 바뀌어 편당 +$0.15, +60~90초.
- 분모도 1이다(collectFullText 가 빈 줄 없이 합쳐 단락 1개로 센다). 토큰 기준을 끄면 분모는 판정에 쓰이지 않는다.
- 처리: 토큰 기준은 `citationTokensExpected` 를 켠 호출자에게만(현재 호출자 없음). 일반론 횟수·RAG overlap 은 유지. 커밋 후 배치 완료 뒤 빌드 → Phase 4 재측정에서 재생성률·비용으로 확인.
- 이 항목은 설계도와 별개의 순수 비용 버그다. 설계도는 "재생성 없이 첫 초안이 통과"를 노리고, 이 픽스는 "통과할 수 없는 재생성"을 없앤다.

### 6.6 Phase 0 기준선 확정 (09-04 02:32~03:20, 16편, GPT-5.6 Terra, 설계도 없음)

| 지표 | 홈판 8편 | SEO 8편 | 전체 16편 |
|---|---|---|---|
| 편당 비용 | $0.379 | $0.320 | $0.349 (총 $5.59) |
| 게이트 pass | 7/8 | 6/8 | 13/16 |
| 모드 점수 평균 / 종합 평균 | 90 / 89 | 86 / 86 | — |
| 유료 재생성(Faithfulness 토큰 밀도) | 8/8 | 8/8 | 16/16 |
| 인용 ≥1 | 8/8 | 4/8 | 12/16 |
| 도입부 상황 단서 | 8/8 | 4/8 | 12/16 |
| 문단당 문장 | 2.0 | 3.4 | 2.7 |
| 본문 글자(공백 제외) | 2,204 | 1,746 | — |
| 소요 | 195s | 147s | 171s |
| 캐시 적중 | 22% | 34% | 28% (편 간 0, 편 내 재시도만) |

- 미달 3편: 홈판 6(중고차, 75) · SEO 1(청년도약계좌, 69) · SEO 5(김치속 만들기, 70). 김치속처럼 기사가 없는 생활 주제는 자료 자체가 얇다(§5 참고).
- SEO 는 인용·상황 단서가 절반. 홈판은 게이트가 두 가지를 직접 요구해 재생성으로 맞춘 결과라 비용이 더 든다.
- 원본 자료: `.autopus/specs/SPEC-BLUEPRINT-2026/baseline.json`, 로그 `tmp/one-article/batch-baseline/`.

### 6.7 설계도 라이브 첫 결과 + 세 번째 강제 재생성 원인 (09-04 03:25~03:35)

- 첫 라이브 설계도(gpt-4.1-mini): 출력 상한 2,048 토큰에 JSON 이 잘려 "읽을 수 없어 생략". 라우트에 maxTokens 옵션을 뚫고 4,096 요청 + 잘린 JSON 복구(4255f744). 재실행: `인용 4·사실 2·소제목 5·제외 6 (버림: 인용 1·사실 6) · 11.7초`, 재료 1,062자.
- 설계도가 사실 목록 prepend 를 대체하자 **첫 본문 호출부터 cachedTokens 18,101 / 33,524** — 편 간 캐시가 살아났다(기준선은 0).
- 인용 밀도 재시도가 사라지자 그 뒤에 숨어 있던 재시도가 드러났다: `[Fidelity] Phase 7-B 자동 재시도: 압축률 15% (임계 50%)`. 압축률 = 본문 길이 / rawText 길이. 키워드 모드 rawText 는 여러 출처 15~45K자라 3,000자 본문은 7~34% 로 항상 미달. 기준선 로그에서도 `Source Fidelity 미달: 압축률 15~34%` 가 매 편 찍혀 있었다. URL 모드(원문 한 편 재구성)에만 뜻이 있는 기준이라 키워드 모드에서는 끈다(4de15b0c). 환각 표지 재시도는 전 모드 유지.
- 정리: 기준선의 "재생성 16/16" 은 품질 미달이 아니라 **통과할 수 없는 두 기준**(금지된 토큰 밀도, 다출처 압축률)이 만든 것이었다. 둘 다 유료 보정을 켠 09-03 이후 비용으로 바뀌었다.

### 6.8 네 번째 재생성 원인 — 환각 "경고"의 정체 (09-04 03:48, 재측정 1~5편)

재측정에서 두 통과 불가 기준이 사라지자 남은 재생성은 90 게이트의 `publication criticalSafety HALLUCINATION_SIGNAL`. 그 신호의 실체를 32편 로그에서 세어 보니:

| 경고 문구 | 건수 |
|---|---|
| 원본의 긍정 키워드 N개 결과에 누락(기부·나눔·선한·감동·희망…) | 8 |
| 감정 방향 mismatch: 원본=긍정 → 결과=긍정 (비율 차이) | 1 |

- 다출처 자료에 섞인 미담 조각의 어휘가 본문에 없다고 "환각"이 된다. 경고 하나가 안전 세부점수 25→12 로 내려가 critical 대역(<25)에 들어가고, 90 게이트는 이를 "해소되지 않은 사실 안전 신호"로 보고 재생성을 산다(quality90Gate 의 이 규칙은 의도된 핀이라 손대지 않는다).
- 처리: 누락은 환각이 아니므로 경고 목록에서 제거(결과 필드는 유지), 방향 경고는 실제로 방향이 뒤집힐 때만(커밋 별도). 강한 환각 판정은 그대로.
- 재측정 배치는 이 픽스 전 dist 로 돌고 있어 이 재생성이 포함된다. 다음 배치에서 확인.
- 재측정 홈판 5편(청년월세지원)이 해악을 그대로 보여 준다: 1차 초안 92/92 pass → 긍정 어휘 누락 "경고"로 재생성 → 2차 80/77(안전 35, decision=regenerate). 가짜 신호가 $0.15 를 쓰고 더 나쁜 글을 골랐다.

### 6.9 Phase 4 재측정 1차 (09-04 03:38~04:07, 같은 16 키워드, 설계도 ON + 픽스 2건: 토큰 밀도·압축률)

| 지표 | 기준선 | 재측정 1차 |
|---|---|---|
| 편당 비용 | $0.349 | **$0.200** (총 $3.19) |
| 캐시 적중 | 28% | 49% |
| 호출 수 | 56 | 40 |
| 게이트 pass | 13/16 | 14/16 |
| 모드 점수 / 종합 평균 | 88 / 87 | 91 / 91 |
| 재생성 | 16/16 | 8/16 (통과 불가 재시도 0) |
| 인용 ≥1 / 도입부 상황 단서 | 12 / 12 | 15 / 14 |
| 설계도 성공 | — | 16/16 (평균 12.8초) |
| 본문 글자 평균 | 1,975 | 1,602 |
| 소요 | 171s | 116s |

- 편별 종합: h1 81→91 · h5 87→77(가짜 환각 경고 재생성) · h6 75→93 · s1 69→73 · s5 70→88(김치속) · s4 92→98 · s7 91→98.
- 본문이 평균 370자 짧아졌다. 설계도가 제외 주제를 명시하고 사실 발췌가 적게 남은(스냅 전) 영향으로 보이나 실측 미확인 — 2차 재측정(스냅 반영)에서 본문 길이를 다시 본다.
- 이 배치는 환각 경고·스냅 픽스 **이전** dist. 남은 재생성 8건의 사유는 별도 집계.

### 6.10 설계도 파서 보강 3단 (09-04 04:15~04:30, 오프라인 재검 — 추가 호출 없음)

2차 재측정 1편(용혜인) RAW 응답을 파서 버전별로 다시 읽은 결과.

| 파서 | 인용 유지 | 사실 유지 |
|---|---|---|
| 정규화 완전일치 | 3/5 | 0/8 |
| 앞뒤 10자 스냅 | 3/5 | 3/8 |
| 8자 조각 투표(55% 이상) + 끝 조각 앵커 | 3/5 | 5/8 |

- 남은 3건은 모델이 자료의 두 문장을 한 문장으로 합성한 발췌(조각 적중 9/18)라 버리는 것이 맞다. 자료 문장이 아닌 것을 "근거"로 실을 수 없다.
- 별도로 2차 재측정 첫 두 편이 통째로 생략된 원인: 모델이 `…없다"고 밝혔다` 를 이스케이프 없이 JSON 문자열에 넣음. 문자열 안 따옴표는 다음 비공백 문자가 `, } ] :` 일 때만 닫는 따옴표로 본다(8184c1d3).

### 6.11 Phase 4 재측정 2차 (09-04 04:20~04:44, 환각 경고 픽스 반영 · 스냅 v1 빌드)


**전체**

| 지표 | baseline | after | after2 |
|---|---|---|---|
| 편수 | 16 | 16 | 16 |
| 편당 비용 | $0.349 (총 $5.59) | $0.200 (총 $3.19) | $0.129 (총 $2.07) |
| 캐시 적중 | 28% | 49% | 49% |
| 호출 수 | 56 | 40 | 25 |
| 게이트 pass | 13/16 | 14/16 | 14/16 |
| 모드 / 종합 평균(게이트 실행 편) | 88 / 87 (16편) | 91 / 91 (16편) | 91 / 94 (14편) |
| 재생성 | 16/16 | 8/16 | 0/16 |
| 통과 불가 재시도(토큰 밀도) | 16/16 | 0/16 | 0/16 |
| 설계도 성공 | 0/16 | 16/16 | 16/16 |
| 인용 ≥1 | 12/16 | 15/16 | 14/16 |
| 도입부 상황 단서 | 12/16 | 14/16 | 14/16 |
| 문단당 문장 | 2.7 | 2.8 | 2.5 |
| 본문 글자(공백 제외) | 1975 | 1602 | 1548 |
| 소요(초) | 171 | 116 | 77 |

**홈판**

| 지표 | baseline | after | after2 |
|---|---|---|---|
| 편수 | 8 | 8 | 8 |
| 편당 비용 | $0.379 (총 $3.03) | $0.189 (총 $1.52) | $0.130 (총 $1.04) |
| 캐시 적중 | 22% | 47% | 47% |
| 호출 수 | 34 | 21 | 15 |
| 게이트 pass | 7/8 | 7/8 | 8/8 |
| 모드 / 종합 평균(게이트 실행 편) | 90 / 89 (8편) | 90 / 91 (8편) | 92 / 94 (8편) |
| 재생성 | 8/8 | 3/8 | 0/8 |
| 통과 불가 재시도(토큰 밀도) | 8/8 | 0/8 | 0/8 |
| 설계도 성공 | 0/8 | 8/8 | 8/8 |
| 인용 ≥1 | 8/8 | 8/8 | 7/8 |
| 도입부 상황 단서 | 8/8 | 8/8 | 8/8 |
| 문단당 문장 | 2.0 | 2.0 | 2.1 |
| 본문 글자(공백 제외) | 2204 | 1788 | 1717 |
| 소요(초) | 195 | 122 | 83 |

**SEO**

| 지표 | baseline | after | after2 |
|---|---|---|---|
| 편수 | 8 | 8 | 8 |
| 편당 비용 | $0.320 (총 $2.56) | $0.210 (총 $1.68) | $0.128 (총 $1.03) |
| 캐시 적중 | 34% | 51% | 51% |
| 호출 수 | 22 | 19 | 10 |
| 게이트 pass | 6/8 | 7/8 | 6/8 |
| 모드 / 종합 평균(게이트 실행 편) | 86 / 86 (8편) | 91 / 90 (8편) | 91 / 94 (6편) |
| 재생성 | 8/8 | 5/8 | 0/8 |
| 통과 불가 재시도(토큰 밀도) | 8/8 | 0/8 | 0/8 |
| 설계도 성공 | 0/8 | 8/8 | 8/8 |
| 인용 ≥1 | 4/8 | 7/8 | 7/8 |
| 도입부 상황 단서 | 4/8 | 6/8 | 6/8 |
| 문단당 문장 | 3.4 | 3.6 | 2.9 |
| 본문 글자(공백 제외) | 1746 | 1415 | 1379 |
| 소요(초) | 147 | 110 | 71 |

편별 종합점수(baseline→after2): h1:81→87 h2:96→96 h3:96→94 h4:89→95 h5:87→98 h6:75→94 h7:94→97 h8:95→94 s1:69→null s2:96→89 s3:87→95 s4:92→null s5:70→96 s6:94→93 s7:91→94 s8:85→98

- 2차에서 게이트 미실행 2편(SEO 1 청년도약계좌 1,142자 · SEO 4 전세보증보험 1,188자): `QualityGate90 권장 분량 미달 결과를 경고와 함께 반환` — 본문이 권장 1,875자(요청 2,500자의 75%) 아래면 모드 게이트를 건너뛰는 기존 동작. 평균은 게이트 실행 편만으로 낸다.
- **본문 길이 추세**: 전체 1,975 → 1,602 → 1,548자, SEO 1,746 → 1,379자. 30개 사실 목록("다 다뤘는지로 판단")을 5~8개 설계도로 바꾼 뒤 모델이 덜 쓴다. 설계도 블록에 "최소선이다, 자료의 다른 사실도 담아 분량을 채운다" 줄을 넣고 factsMax 8→10 으로(다음 빌드). 4편 스팟체크로 확인.
- 설계도 유지율(라이브 스냅 v1): 16편 인용 56(버림 12)·사실 86(버림 40). 같은 RAW 를 스냅 v2 로 재해석: 인용 62(버림 6)·사실 108(버림 18).
- 예산 실사용: 기준선 $5.59 + 1차 $3.19 + 2차 $2.07 + 스모크 $0.34 + 중단 런 약 $0.9 ≈ **$12.1**.
