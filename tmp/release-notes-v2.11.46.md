# v2.11.46

## 발행 안정성
- 발행 직전 검사가 **정상 글을 잘못 차단하고 튕기던 문제** 수정 — 에디터 본문을 component/root/fallback 중 가장 완전한 스냅샷으로 측정하고, 짧게 읽힌 경우 안정 대기·프레임 재획득으로 재측정해 확정된 경우에만 차단(누락 발행 방지는 유지).
- 계정 전환 시 입력해둔 **키워드·URL이 초기화되던 문제** 수정 — 전환(세션 clear/restore)에도 보존.

## UI 정리 (미사용 기능 제거)
- **URL 이미지 자동 수집** 기능·UI 제거 (연속발행·다계정).
- **앱 스케줄 모드** 제거 — 예약발행은 **네이버 서버 예약**으로 일원화(날짜/시간 예약 UI 유지).
- **AI 영상(VEO)** 생성 기능 UI 제거 (사용법 영상 탭은 유지).
- **AI 이미지 관련성 검증** 제거 (UI + 백엔드).
- 소제목 분석 영역의 **중복 "썸네일 텍스트 포함" 체크박스** 제거.
- 이미지 소스 선택에서 **내 폴더 / 저장된 이미지**를 AI 엔진 드롭다운 밖 별도 버튼으로 분리.

## dropshot(리더스 나노바나나 무제한)
- **로그인 인식 수정** — 로그인 신호를 쿠키의 살아있는 세션 토큰(CognitoIdentityServiceProvider `.idToken`/`.accessToken`)으로 정확히 판정. 이전엔 로그아웃 후에도 남는 키를 인정해 오탐("로그인 안 했는데 완료")·로그인 창 즉시 닫힘이 있었음.

## 이미지 품질
- 나노바나나2 등 인물 이미지의 **해부학 오류(손가락 등) 완화** — 프롬프트에 손/팔/머리 정상 묘사 지시 추가(모델 출력 특성상 발생률 완화).

---
검증: vitest 3,470 GREEN · lint 0 errors · build PASS.
